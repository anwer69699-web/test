repeat task.wait() until game:IsLoaded()

-- ═══════════════════════════════════════════════════════════════
-- NUXUS HUB - FIXED VERSION
-- ═══════════════════════════════════════════════════════════════

local Services = {
    Players = game:GetService("Players"),
    RunService = game:GetService("RunService"),
    CoreGui = game:GetService("CoreGui"),
    TweenService = game:GetService("TweenService"),
    UserInputService = game:GetService("UserInputService"),
    HttpService = game:GetService("HttpService"),
    Stats = game:GetService("Stats"),
    Debris = game:GetService("Debris"),
}

local Player = Services.Players.LocalPlayer

local isPC = Services.UserInputService.KeyboardEnabled and Services.UserInputService.MouseEnabled

local Viewport = workspace.CurrentCamera.ViewportSize
local Scale = math.clamp(math.min(Viewport.X / 1920, Viewport.Y / 1080), 0.6, 1.2)

local TH = {
    bg = Color3.fromRGB(12, 6, 8),
    headerBg = Color3.fromRGB(28, 12, 18),
    rowBg = Color3.fromRGB(22, 10, 14),
    accent = Color3.fromRGB(255, 50, 80),
    accentGlow = Color3.fromRGB(255, 100, 130),
    textW = Color3.fromRGB(255, 255, 255),
    textD = Color3.fromRGB(200, 150, 160),
    danger = Color3.fromRGB(255, 45, 75),
    borderDark = Color3.fromRGB(60, 20, 30),
    red = Color3.fromRGB(255, 60, 90),
    redBright = Color3.fromRGB(255, 80, 100),
    redDark = Color3.fromRGB(180, 40, 60),
    redGlow = Color3.fromRGB(255, 120, 150),
    green = Color3.fromRGB(80, 220, 120),
}

local NamePool = {
    Gui = {"Nuxus_Hub","System_Core","Data_Manager","UI_Controller","Event_Handler","Net_Utils","Data_Sync","Event_Bus","Render_Module","View_Handler","Ctrl_Panel","Core_Process"},
    Frame = {"Main_Panel","Control_Center","Interface_V2","View_Port","Display_Area","Config_View","Overlay_UI","Panel_V3","Sys_Widget","Hub_Frame","Info_Bar","Stat_Panel"},
    Part = {"EnvCollider","WorldProp","ZoneMarker","AreaTrigger","GroundTile","PhysicsObj"},
}

local function rotateName(pool, category)
    local base = pool[math.random(1, #pool)]
    local suffix = tostring(math.random(1000, 9999))
    return base .. "_" .. suffix
end

-- ═══════════════════════════════════════════════
-- FORWARD DECLARATIONS (متغيرات معرفة مسبقاً)
-- ═══════════════════════════════════════════════

local SpeedBoostActive = false
local LockActive = false
local pathActive = false
local AntiRagdollEnabled = false
local VisualActive = false
local UnwalkActive = false
local DropActive = false
local ARConns = {}
local DropConns = {}
local savedWalkSpeed = 16
local GuiRef = nil
local WatermarkGui = nil
local RadiusBall = nil
local SoundParent = nil
local FPSConn = nil
local AutoStealConn = nil
local SpeedBoostConn = nil
local LockConn = nil
local RadiusBallConn = nil
local jumpConnection = nil
local scriptAlive = true

-- ═══════════════════════════════════════════════
-- UTILITY FUNCTIONS (دوال مساعدة - معرفة مبكراً)
-- ═══════════════════════════════════════════════

local function getCharacter()
    return Player.Character
end

local function getHRP()
    local char = getCharacter()
    if not char then return nil end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if hrp and hrp:IsA("BasePart") then return hrp end
    return nil
end

local function getHumanoid()
    local char = getCharacter()
    if not char then return nil end
    local hum = char:FindFirstChildWhichIsA("Humanoid")
    if hum and hum.Health > 0 then return hum end
    return nil
end

local function isAlive()
    local hum = getHumanoid()
    return hum ~= nil and hum.Health > 0
end

-- ═══════════════════════════════════════════════
-- SOUND SYSTEM
-- ═══════════════════════════════════════════════

pcall(function()
    SoundParent = Instance.new("Folder", game:GetService("ReplicatedStorage"))
    SoundParent.Name = "NuxusAudio"
end)
if not SoundParent then
    SoundParent = Instance.new("Folder", workspace)
    SoundParent.Name = rotateName(NamePool.Part, "Audio")
end

local function createSound(id, vol, pitch)
    local s = Instance.new("Sound")
    s.SoundId = "rbxassetid://" .. tostring(id)
    s.Volume = vol or 0.3
    s.PlaybackSpeed = pitch or 1
    s.Parent = SoundParent
    return s
end

local SFX = {
    hover = createSound(876939936, 0.15, 1.2),
    click = createSound(876939830, 0.25, 1.1),
    toggleOn = createSound(876940129, 0.3, 1.0),
    toggleOff = createSound(876940274, 0.25, 0.9),
    dragStart = createSound(6895079853, 0.1, 0.8),
    warn = createSound(6895079853, 0.4, 0.6),
}

local lastHoverTime = 0

local function playSFX(name)
    local s = SFX[name]
    if s and s.Parent then
        pcall(function()
            s:Stop()
            s:Play()
        end)
    end
end

local function playHover()
    local now = tick()
    if now - lastHoverTime < 0.08 then return end
    lastHoverTime = now
    playSFX("hover")
end

local function addBtnEffects(btn)
    local origBgTransparency = 0.5
    local bg = btn:FindFirstChild("BG")
    if bg then origBgTransparency = bg.BackgroundTransparency end
    
    btn.MouseEnter:Connect(function()
        playHover()
        if bg then
            origBgTransparency = bg.BackgroundTransparency
            Services.TweenService:Create(bg, TweenInfo.new(0.15), {
                BackgroundTransparency = math.max(0, origBgTransparency - 0.2)
            }):Play()
        end
        Services.TweenService:Create(btn, TweenInfo.new(0.1, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
            TextTransparency = 0
        }):Play()
    end)
    btn.MouseLeave:Connect(function()
        if bg then
            Services.TweenService:Create(bg, TweenInfo.new(0.2), {
                BackgroundTransparency = origBgTransparency
            }):Play()
        end
    end)
end

-- ═══════════════════════════════════════════════
-- UI HELPERS
-- ═══════════════════════════════════════════════

local function createAnimatedBorder(parent, speed, thickness)
    speed = speed or 1.5
    thickness = thickness or 2
    local stroke = Instance.new("UIStroke")
    stroke.Thickness = thickness
    stroke.Color = Color3.new(1, 1, 1)
    stroke.Transparency = 0
    stroke.Parent = parent
    local grad = Instance.new("UIGradient")
    grad.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, TH.borderDark),
        ColorSequenceKeypoint.new(0.15, TH.redGlow),
        ColorSequenceKeypoint.new(0.3, TH.borderDark),
        ColorSequenceKeypoint.new(0.5, TH.red),
        ColorSequenceKeypoint.new(0.7, TH.borderDark),
        ColorSequenceKeypoint.new(0.85, TH.redBright),
        ColorSequenceKeypoint.new(1, TH.borderDark),
    })
    grad.Rotation = 0
    grad.Parent = stroke
    task.spawn(function()
        while parent and parent.Parent do
            grad.Rotation = (grad.Rotation + speed) % 360
            task.wait(0.033)
        end
    end)
    return stroke
end

local function createAnimatedTextGradient(textLabel, speed)
    speed = speed or 2
    local grad = Instance.new("UIGradient")
    grad.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, TH.redDark),
        ColorSequenceKeypoint.new(0.25, TH.redBright),
        ColorSequenceKeypoint.new(0.5, TH.redGlow),
        ColorSequenceKeypoint.new(0.75, TH.redBright),
        ColorSequenceKeypoint.new(1, TH.redDark),
    })
    grad.Rotation = 0
    grad.Parent = textLabel
    task.spawn(function()
        while textLabel and textLabel.Parent do
            grad.Rotation = (grad.Rotation + speed) % 360
            task.wait(0.033)
        end
    end)
    return grad
end

local function makeCorner(parent, radius)
    local c = Instance.new("UICorner", parent)
    c.CornerRadius = UDim.new(0, radius or 10)
    return c
end

local function makeBtn(parent, x, y, w, h, text, fontSize)
    fontSize = fontSize or 10
    local btn = Instance.new("TextButton", parent)
    btn.Size = UDim2.new(0, w * Scale, 0, h * Scale)
    btn.Position = UDim2.new(0, x * Scale, 0, y * Scale)
    btn.BackgroundTransparency = 1
    btn.BorderSizePixel = 0
    btn.Text = text or ""
    btn.TextColor3 = TH.redBright
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = fontSize * Scale
    btn.AutoButtonColor = false
    createAnimatedBorder(btn, 1.5, 1)
    createAnimatedTextGradient(btn, 2)

    local bg = Instance.new("Frame", btn)
    bg.Name = "BG"
    bg.Size = UDim2.new(1, 0, 1, 0)
    bg.BackgroundColor3 = TH.rowBg
    bg.BackgroundTransparency = 0.5
    bg.BorderSizePixel = 0
    bg.ZIndex = -1
    makeCorner(bg, 5)

    addBtnEffects(btn)

    btn.MouseButton1Click:Connect(function()
        playSFX("click")
    end)

    return btn
end

local function makeStyledBox(parent, x, y, w, h, text)
    local box = Instance.new("TextBox", parent)
    box.Size = UDim2.new(0, w * Scale, 0, h * Scale)
    box.Position = UDim2.new(0, x * Scale, 0, y * Scale)
    box.BackgroundTransparency = 1
    box.Text = text or ""
    box.Font = Enum.Font.GothamBold
    box.TextSize = 11 * Scale
    box.TextColor3 = TH.redBright
    box.ClearTextOnFocus = false
    createAnimatedBorder(box, 1.5, 1)
    createAnimatedTextGradient(box, 2)

    local bg = Instance.new("Frame", box)
    bg.Name = "BG"
    bg.Size = UDim2.new(1, 0, 1, 0)
    bg.BackgroundColor3 = TH.rowBg
    bg.BackgroundTransparency = 0.5
    bg.BorderSizePixel = 0
    bg.ZIndex = -1
    makeCorner(bg, 4)

    return box
end

local function makeStyledLabel(parent, x, y, w, h, text, fontSize)
    fontSize = fontSize or 10
    local lbl = Instance.new("TextLabel", parent)
    lbl.Size = UDim2.new(0, w * Scale, 0, h * Scale)
    lbl.Position = UDim2.new(0, x * Scale, 0, y * Scale)
    lbl.BackgroundTransparency = 1
    lbl.Text = text or ""
    lbl.TextColor3 = TH.redBright
    lbl.Font = Enum.Font.GothamBold
    lbl.TextSize = fontSize * Scale
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    createAnimatedTextGradient(lbl, 2)
    return lbl
end

local function makeHeader(parent, titleText)
    local header = Instance.new("Frame", parent)
    header.Size = UDim2.new(1, 0, 0, 22 * Scale)
    header.BackgroundColor3 = TH.headerBg
    header.BackgroundTransparency = 1
    header.BorderSizePixel = 0
    makeCorner(header, 10)
    local title = Instance.new("TextLabel", header)
    title.Size = UDim2.new(1, -20 * Scale, 1, 0)
    title.Position = UDim2.new(0, 8 * Scale, 0, 0)
    title.BackgroundTransparency = 1
    title.Text = titleText
    title.Font = Enum.Font.GothamBold
    title.TextSize = 11 * Scale
    title.TextColor3 = TH.redBright
    title.ZIndex = 2
    createAnimatedTextGradient(title, 2)
    return header, title
end

local function setBtnBg(btn, color)
    local bg = btn:FindFirstChild("BG")
    if bg then bg.BackgroundColor3 = color end
end

local function makeDraggable(btn, clickHandler)
    local dragging = false
    local dragConn = nil
    local moveConn = nil
    local dragStart = Vector3.zero
    local startPos = UDim2.new()
    local startMousePos = Vector3.zero
    local totalDragDist = 0

    btn.Active = true

    btn.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then return end
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            totalDragDist = 0
            dragStart = input.Position
            startMousePos = input.Position
            startPos = btn.Position
            playSFX("dragStart")

            if dragConn then dragConn:Disconnect() end
            if moveConn then moveConn:Disconnect() end

            dragConn = Services.UserInputService.InputEnded:Connect(function(endInput)
                if dragging and (endInput.UserInputType == Enum.UserInputType.MouseButton1 or endInput.UserInputType == Enum.UserInputType.Touch) then
                    local endDist = (endInput.Position - dragStart).Magnitude
                    if totalDragDist < 10 and endDist < 10 then
                        if clickHandler then clickHandler() end
                    end
                    dragging = false
                    if dragConn then dragConn:Disconnect(); dragConn = nil end
                    if moveConn then moveConn:Disconnect(); moveConn = nil end
                end
            end)

            moveConn = Services.UserInputService.InputChanged:Connect(function(input)
                if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                    local delta = input.Position - startMousePos
                    totalDragDist = totalDragDist + delta.Magnitude
                    btn.Position = UDim2.new(
                        startPos.X.Scale, startPos.X.Offset + delta.X,
                        startPos.Y.Scale, startPos.Y.Offset + delta.Y
                    )
                end
            end)
        end
    end)
end

local function makeFrameDraggableByHeader(frame, headerFrame)
    frame.Active = true
    local dragging = false
    local dragConn = nil
    local moveConn = nil
    local startPos = UDim2.new()
    local startMousePos = Vector3.zero

    headerFrame.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then return end
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            startMousePos = input.Position
            startPos = frame.Position
            playSFX("dragStart")

            if dragConn then dragConn:Disconnect() end
            if moveConn then moveConn:Disconnect() end

            dragConn = Services.UserInputService.InputEnded:Connect(function(endInput)
                if dragging and (endInput.UserInputType == Enum.UserInputType.MouseButton1 or endInput.UserInputType == Enum.UserInputType.Touch) then
                    dragging = false
                    if dragConn then dragConn:Disconnect(); dragConn = nil end
                    if moveConn then moveConn:Disconnect(); moveConn = nil end
                end
            end)

            moveConn = Services.UserInputService.InputChanged:Connect(function(input)
                if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                    local delta = input.Position - startMousePos
                    frame.Position = UDim2.new(
                        startPos.X.Scale, startPos.X.Offset + delta.X,
                        startPos.Y.Scale, startPos.Y.Offset + delta.Y
                    )
                end
            end)
        end
    end)
end

-- ═══════════════════════════════════════════════
-- KEY SYSTEM
-- ═══════════════════════════════════════════════

local TRIAL_KEY = "Nuxus-Trial"
local TRIAL_DURATION = 90

local RealKeys = {
    ["NX-K1Pm9q"] = 0,
    ["NX-K2Rw7x"] = 0,
    ["NX-K3Tn4b"] = 0,
    ["NX-K4Vs8j"] = 0,
    ["NX-K5Lc2d"] = 0,
    ["NX-K6Fh6k"] = 0,
    ["NX-K7Yg3m"] = 0,
    ["NX-K8Qe5p"] = 0,
    ["NX-K9Ua1r"] = 0,
    ["NX-K10Bz0w"] = 0,
}

local isTrialUser = false
local myUserId = Player.UserId

local keyGui = Instance.new("ScreenGui")
keyGui.Name = rotateName(NamePool.Gui, "KeyGui")
keyGui.ResetOnSpawn = false
keyGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
keyGui.Parent = Services.CoreGui

local keyFrameW = 300
local keyFrameH = 240

local KeyMainFrame = Instance.new("Frame", keyGui)
KeyMainFrame.Size = UDim2.new(0, keyFrameW * Scale, 0, keyFrameH * Scale)
KeyMainFrame.Position = UDim2.new(0.5, -(keyFrameW * Scale) / 2, 0.5, -(keyFrameH * Scale) / 2)
KeyMainFrame.BackgroundColor3 = TH.bg
KeyMainFrame.BackgroundTransparency = 0.15
KeyMainFrame.BorderSizePixel = 0
makeCorner(KeyMainFrame, 12)
createAnimatedBorder(KeyMainFrame, 1.5, 2)

local KeyTitle = Instance.new("TextLabel", KeyMainFrame)
KeyTitle.Size = UDim2.new(0, 260 * Scale, 0, 28 * Scale)
KeyTitle.Position = UDim2.new(0.5, -(260 * Scale) / 2, 0, 20 * Scale)
KeyTitle.BackgroundTransparency = 1
KeyTitle.Text = "Nuxus Hub"
KeyTitle.Font = Enum.Font.GothamBlack
KeyTitle.TextSize = 22 * Scale
KeyTitle.TextColor3 = TH.redBright
KeyTitle.ZIndex = 2
createAnimatedTextGradient(KeyTitle, 2)

local KeySubtitle = Instance.new("TextLabel", KeyMainFrame)
KeySubtitle.Size = UDim2.new(0, 260 * Scale, 0, 18 * Scale)
KeySubtitle.Position = UDim2.new(0.5, -(260 * Scale) / 2, 0, 52 * Scale)
KeySubtitle.BackgroundTransparency = 1
KeySubtitle.Text = "Key System"
KeySubtitle.Font = Enum.Font.GothamBold
KeySubtitle.TextSize = 14 * Scale
KeySubtitle.TextColor3 = TH.redGlow
KeySubtitle.ZIndex = 2
createAnimatedTextGradient(KeySubtitle, 2.5)

local KeyDivider = Instance.new("Frame", KeyMainFrame)
KeyDivider.Size = UDim2.new(0, 200 * Scale, 0, 1 * Scale)
KeyDivider.Position = UDim2.new(0.5, -(200 * Scale) / 2, 0, 72 * Scale)
KeyDivider.BackgroundColor3 = TH.redDark
KeyDivider.BackgroundTransparency = 0.3
KeyDivider.BorderSizePixel = 0

local TrialKeyLbl = Instance.new("TextLabel", KeyMainFrame)
TrialKeyLbl.Size = UDim2.new(0, 240 * Scale, 0, 20 * Scale)
TrialKeyLbl.Position = UDim2.new(0.5, -(240 * Scale) / 2, 0, 82 * Scale)
TrialKeyLbl.BackgroundTransparency = 1
TrialKeyLbl.Text = "90s key: " .. TRIAL_KEY
TrialKeyLbl.Font = Enum.Font.GothamBold
TrialKeyLbl.TextSize = 12 * Scale
TrialKeyLbl.TextColor3 = TH.redBright
TrialKeyLbl.ZIndex = 2

local KeyInputBox = Instance.new("TextBox", KeyMainFrame)
KeyInputBox.Size = UDim2.new(0, 220 * Scale, 0, 28 * Scale)
KeyInputBox.Position = UDim2.new(0.5, -(220 * Scale) / 2, 0, 110 * Scale)
KeyInputBox.BackgroundTransparency = 1
KeyInputBox.PlaceholderText = "Enter your key"
KeyInputBox.PlaceholderColor3 = TH.textD
KeyInputBox.Text = ""
KeyInputBox.Font = Enum.Font.GothamBold
KeyInputBox.TextSize = 13 * Scale
KeyInputBox.TextColor3 = TH.textW
KeyInputBox.ClearTextOnFocus = false
createAnimatedBorder(KeyInputBox, 1.5, 1)

local KeyInputBg = Instance.new("Frame", KeyInputBox)
KeyInputBg.Name = "BG"
KeyInputBg.Size = UDim2.new(1, 0, 1, 0)
KeyInputBg.BackgroundColor3 = TH.rowBg
KeyInputBg.BackgroundTransparency = 0.3
KeyInputBg.BorderSizePixel = 0
KeyInputBg.ZIndex = -1
makeCorner(KeyInputBg, 6)

local keyEvent = Instance.new("BindableEvent")

local KeySubmitBtn = Instance.new("TextButton", KeyMainFrame)
KeySubmitBtn.Size = UDim2.new(0, 140 * Scale, 0, 30 * Scale)
KeySubmitBtn.Position = UDim2.new(0.5, -(140 * Scale) / 2, 0, 146 * Scale)
KeySubmitBtn.BackgroundTransparency = 1
KeySubmitBtn.BorderSizePixel = 0
KeySubmitBtn.Text = "Submit"
KeySubmitBtn.TextColor3 = TH.textW
KeySubmitBtn.Font = Enum.Font.GothamBold
KeySubmitBtn.TextSize = 13 * Scale
KeySubmitBtn.AutoButtonColor = false
createAnimatedBorder(KeySubmitBtn, 1.5, 1)
createAnimatedTextGradient(KeySubmitBtn, 2)

local KeySubmitBg = Instance.new("Frame", KeySubmitBtn)
KeySubmitBg.Name = "BG"
KeySubmitBg.Size = UDim2.new(1, 0, 1, 0)
KeySubmitBg.BackgroundColor3 = TH.redDark
KeySubmitBg.BackgroundTransparency = 0.2
KeySubmitBg.BorderSizePixel = 0
KeySubmitBg.ZIndex = -1
makeCorner(KeySubmitBg, 6)

local AttemptsLabel = Instance.new("TextLabel", KeyMainFrame)
AttemptsLabel.Size = UDim2.new(0, 260 * Scale, 0, 16 * Scale)
AttemptsLabel.Position = UDim2.new(0.5, -(260 * Scale) / 2, 0, 184 * Scale)
AttemptsLabel.BackgroundTransparency = 1
AttemptsLabel.Text = "Attempts remaining: 3"
AttemptsLabel.Font = Enum.Font.GothamBold
AttemptsLabel.TextSize = 10 * Scale
AttemptsLabel.TextColor3 = TH.textD
AttemptsLabel.ZIndex = 2

local KeyStatusLabel = Instance.new("TextLabel", KeyMainFrame)
KeyStatusLabel.Size = UDim2.new(0, 260 * Scale, 0, 16 * Scale)
KeyStatusLabel.Position = UDim2.new(0.5, -(260 * Scale) / 2, 0, 204 * Scale)
KeyStatusLabel.BackgroundTransparency = 1
KeyStatusLabel.Text = ""
KeyStatusLabel.Font = Enum.Font.GothamBold
KeyStatusLabel.TextSize = 11 * Scale
KeyStatusLabel.TextColor3 = TH.textW
KeyStatusLabel.ZIndex = 2

local KeyCloseBtn = Instance.new("TextButton", KeyMainFrame)
KeyCloseBtn.Size = UDim2.new(0, 24 * Scale, 0, 24 * Scale)
KeyCloseBtn.Position = UDim2.new(1, -28 * Scale, 0, 4 * Scale)
KeyCloseBtn.BackgroundTransparency = 1
KeyCloseBtn.BorderSizePixel = 0
KeyCloseBtn.Text = "X"
KeyCloseBtn.Font = Enum.Font.GothamBold
KeyCloseBtn.TextSize = 12 * Scale
KeyCloseBtn.TextColor3 = TH.redBright
KeyCloseBtn.ZIndex = 10
KeyCloseBtn.AutoButtonColor = false
createAnimatedBorder(KeyCloseBtn, 1.5, 1)

local KeyCloseBg = Instance.new("Frame", KeyCloseBtn)
KeyCloseBg.Name = "BG"
KeyCloseBg.Size = UDim2.new(1, 0, 1, 0)
KeyCloseBg.BackgroundColor3 = Color3.fromRGB(35, 15, 20)
KeyCloseBg.BackgroundTransparency = 0.3
KeyCloseBg.BorderSizePixel = 0
KeyCloseBg.ZIndex = -1
makeCorner(KeyCloseBg, 4)

KeyCloseBtn.MouseButton1Click:Connect(function()
    keyEvent:Fire(false)
end)

local attemptsRemaining = 3

local function submitKey()
    local enteredKey = KeyInputBox.Text
    if enteredKey == "" then return end

    if enteredKey == TRIAL_KEY then
        isTrialUser = true
        KeyStatusLabel.Text = "Trial: 90s only"
        KeyStatusLabel.TextColor3 = Color3.fromRGB(255, 200, 80)
        task.wait(1)
        keyEvent:Fire(true)
        return
    end

    for key, uid in pairs(RealKeys) do
        if enteredKey == key then
            if myUserId == uid then
                KeyStatusLabel.Text = "Key accepted"
                KeyStatusLabel.TextColor3 = TH.green
                task.wait(0.5)
                keyEvent:Fire(true)
                return
            else
                KeyStatusLabel.Text = "Key is not for your account"
                KeyStatusLabel.TextColor3 = TH.danger
                return
            end
        end
    end

    attemptsRemaining = attemptsRemaining - 1
    if attemptsRemaining <= 0 then
        AttemptsLabel.Text = "Attempts remaining: 0"
        KeyStatusLabel.Text = "Locked! Too many attempts."
        KeyStatusLabel.TextColor3 = TH.danger
        KeySubmitBtn.Active = false
        KeyInputBox.Active = false
        KeyInputBox.TextEditable = false
        task.wait(2)
        keyEvent:Fire(false)
    else
        AttemptsLabel.Text = "Attempts remaining: " .. tostring(attemptsRemaining)
        KeyStatusLabel.Text = "Invalid key"
        KeyStatusLabel.TextColor3 = TH.danger
    end
end

KeySubmitBtn.MouseButton1Click:Connect(submitKey)
KeyInputBox.FocusLost:Connect(function(enterPressed)
    if enterPressed then
        submitKey()
    end
end)

-- Music in key screen
local MusicSound = Instance.new("Sound")
MusicSound.SoundId = "rbxassetid://9125304578"
MusicSound.Volume = 0.15
MusicSound.Looped = true
MusicSound.Parent = SoundParent

local MusicBtn = Instance.new("TextButton", keyGui)
MusicBtn.Size = UDim2.new(0, 50 * Scale, 0, 50 * Scale)
MusicBtn.Position = UDim2.new(0, 10 * Scale, 0.5, -25 * Scale)
MusicBtn.BackgroundTransparency = 1
MusicBtn.BorderSizePixel = 0
MusicBtn.Text = "♪"
MusicBtn.TextColor3 = TH.redBright
MusicBtn.Font = Enum.Font.GothamBold
MusicBtn.TextSize = 24 * Scale
MusicBtn.AutoButtonColor = false
MusicBtn.Active = true
createAnimatedBorder(MusicBtn, 1.5, 1)
createAnimatedTextGradient(MusicBtn, 2)

local MusicBtnBg = Instance.new("Frame", MusicBtn)
MusicBtnBg.Name = "BG"
MusicBtnBg.Size = UDim2.new(1, 0, 1, 0)
MusicBtnBg.BackgroundColor3 = TH.rowBg
MusicBtnBg.BackgroundTransparency = 0.5
MusicBtnBg.BorderSizePixel = 0
MusicBtnBg.ZIndex = -1
makeCorner(MusicBtnBg, 8)

local musicPlaying = false
makeDraggable(MusicBtn, function()
    musicPlaying = not musicPlaying
    if musicPlaying then
        pcall(function() MusicSound:Play() end)
        MusicBtn.Text = "♫"
        setBtnBg(MusicBtn, TH.redDark)
    else
        pcall(function() MusicSound:Stop() end)
        MusicBtn.Text = "♪"
        setBtnBg(MusicBtn, TH.rowBg)
    end
end)

task.spawn(function()
    task.wait(0.5)
    pcall(function() MusicSound:Play() end)
    musicPlaying = true
    MusicBtn.Text = "♫"
end)

local keyResult = keyEvent.Event:Wait()

pcall(function()
    if MusicSound then
        MusicSound:Stop()
        MusicSound:Destroy()
    end
end)
keyEvent:Destroy()
keyGui:Destroy()

if not keyResult then
    return
end

-- ═══════════════════════════════════════════════
-- TRIAL SYSTEM
-- ═══════════════════════════════════════════════

local trialRemaining = TRIAL_DURATION

if isTrialUser then
    local trialEnded = false
    local trialPunishActive = false

    task.spawn(function()
        while trialRemaining > 0 and scriptAlive do
            task.wait(1)
            trialRemaining = trialRemaining - 1
        end
        if not scriptAlive then return end

        trialEnded = true
        scriptAlive = false

        -- Warning GUI
        local warnGui = Instance.new("ScreenGui")
        warnGui.Name = "NUXUS_TRIAL_EXPIRED"
        warnGui.ResetOnSpawn = false
        warnGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
        warnGui.DisplayOrder = 9999
        warnGui.Parent = Services.CoreGui

        local blackOverlay = Instance.new("Frame", warnGui)
        blackOverlay.Name = "BlackOverlay"
        blackOverlay.Size = UDim2.new(1, 0, 1, 0)
        blackOverlay.BackgroundColor3 = Color3.new(0, 0, 0)
        blackOverlay.BackgroundTransparency = 0.15
        blackOverlay.BorderSizePixel = 0
        blackOverlay.ZIndex = 1000

        local warnBg = Instance.new("Frame", warnGui)
        warnBg.Size = UDim2.new(0, 380 * Scale, 0, 220 * Scale)
        warnBg.Position = UDim2.new(0.5, -(380 * Scale) / 2, 0.5, -(220 * Scale) / 2)
        warnBg.BackgroundColor3 = Color3.fromRGB(15, 2, 5)
        warnBg.BackgroundTransparency = 0.05
        warnBg.BorderSizePixel = 0
        warnBg.ZIndex = 1001
        makeCorner(warnBg, 12)
        createAnimatedBorder(warnBg, 3, 3)

        local warnTitle = Instance.new("TextLabel", warnBg)
        warnTitle.Size = UDim2.new(1, -20 * Scale, 0, 28 * Scale)
        warnTitle.Position = UDim2.new(0, 10 * Scale, 0, 14 * Scale)
        warnTitle.BackgroundTransparency = 1
        warnTitle.Text = "!! TRIAL EXPIRED !!"
        warnTitle.Font = Enum.Font.GothamBlack
        warnTitle.TextSize = 20 * Scale
        warnTitle.TextColor3 = Color3.new(1, 0, 0)
        warnTitle.ZIndex = 1002

        local warnLine1 = Instance.new("TextLabel", warnBg)
        warnLine1.Size = UDim2.new(1, -24 * Scale, 0, 56 * Scale)
        warnLine1.Position = UDim2.new(0, 12 * Scale, 0, 48 * Scale)
        warnLine1.BackgroundTransparency = 1
        warnLine1.Text = "Trial period has expired.\nScript will be terminated immediately.\nTo purchase, open a ticket on Discord."
        warnLine1.Font = Enum.Font.Gotham
        warnLine1.TextSize = 11 * Scale
        warnLine1.TextColor3 = TH.textD
        warnLine1.TextWrapped = true
        warnLine1.TextXAlignment = Enum.TextXAlignment.Center
        warnLine1.ZIndex = 1002

        local warnDiscord = Instance.new("TextLabel", warnBg)
        warnDiscord.Size = UDim2.new(1, -24 * Scale, 0, 22 * Scale)
        warnDiscord.Position = UDim2.new(0, 12 * Scale, 0, 116 * Scale)
        warnDiscord.BackgroundTransparency = 1
        warnDiscord.Text = "discord.gg/DEFUvkghW7"
        warnDiscord.Font = Enum.Font.GothamBold
        warnDiscord.TextSize = 14 * Scale
        warnDiscord.TextColor3 = TH.redGlow
        warnDiscord.ZIndex = 1002

        local warnTimer = Instance.new("TextLabel", warnBg)
        warnTimer.Size = UDim2.new(1, -20 * Scale, 0, 20 * Scale)
        warnTimer.Position = UDim2.new(0, 10 * Scale, 0, 152 * Scale)
        warnTimer.BackgroundTransparency = 1
        warnTimer.Text = "Terminating in 3..."
        warnTimer.Font = Enum.Font.GothamBold
        warnTimer.TextSize = 13 * Scale
        warnTimer.TextColor3 = TH.danger
        warnTimer.ZIndex = 1002

        -- Disable all features
        SpeedBoostActive = false
        LockActive = false
        pathActive = false
        AntiRagdollEnabled = false
        VisualActive = false
        UnwalkActive = false
        DropActive = false

        -- Cleanup
        pcall(function() if GuiRef and GuiRef.Parent then GuiRef:Destroy() end end)
        pcall(function() if WatermarkGui and WatermarkGui.Parent then WatermarkGui:Destroy() end end)
        pcall(function() if RadiusBall and RadiusBall.Parent then RadiusBall:Destroy() end end)
        pcall(function() if SoundParent and SoundParent.Parent then SoundParent:Destroy() end end)

        pcall(function()
            if FPSConn then FPSConn:Disconnect() end
            if AutoStealConn then AutoStealConn:Disconnect() end
            if SpeedBoostConn then SpeedBoostConn:Disconnect() end
            if LockConn then LockConn:Disconnect() end
            if RadiusBallConn then RadiusBallConn:Disconnect() end
            if jumpConnection then jumpConnection:Disconnect() end
            for _, c in pairs(ARConns) do pcall(function() c:Disconnect() end) end
            for _, c in ipairs(DropConns) do pcall(function() c:Disconnect() end) end
        end)

        ARConns = {}
        DropConns = {}
        GuiRef = nil
        trialPunishActive = true

        -- Punishment loops
        task.spawn(function()
            while trialPunishActive do
                pcall(function() Player:Kick("Trial expired - discord.gg/DEFUvkghW7") end)
                task.wait(1)
            end
        end)
    end)
end

-- ═══════════════════════════════════════════════
-- HOTKEYS & SETTINGS
-- ═══════════════════════════════════════════════

local DiscordLink = "discord.gg/DEFUvkghW7"

WatermarkGui = Instance.new("ScreenGui")
WatermarkGui.Name = rotateName(NamePool.Gui, "WM")
WatermarkGui.ResetOnSpawn = false
WatermarkGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
WatermarkGui.Parent = Services.CoreGui

local WatermarkLbl = Instance.new("TextLabel", WatermarkGui)
WatermarkLbl.Size = UDim2.new(0, 200 * Scale, 0, 18 * Scale)
WatermarkLbl.Position = UDim2.new(0.5, -(200 * Scale) / 2, 0, 1 * Scale)
WatermarkLbl.BackgroundTransparency = 1
WatermarkLbl.Text = DiscordLink
WatermarkLbl.Font = Enum.Font.GothamBold
WatermarkLbl.TextSize = 12 * Scale
WatermarkLbl.TextColor3 = TH.redDark
WatermarkLbl.TextTransparency = 0.4
WatermarkLbl.ZIndex = 1

_G.NuxusHotkeys = _G.NuxusHotkeys or _G.NuseHotkeys or { AutoLeft = "H", AutoRight = "J", Drop = "K", Lock = "L", SpeedBoost = "B", ModeCycle = "N" }
_G.NuxusBooster = _G.NuxusBooster or _G.SpeedBooster or {}

local Hotkeys = _G.NuxusHotkeys

_G.NuxusBooster.Mode = _G.NuxusBooster.Mode or "NORMAL"

local ModeDefaults = {
    NORMAL = { NormalSpeed = 50, StealSpeed = 28 },
    DESYNC = { NormalSpeed = 38, StealSpeed = 18 },
    LAGGER = { NormalSpeed = 35, StealSpeed = 14 },
}

local CustomModeSpeeds = _G.NuxusBooster.CustomModeSpeeds or {}

local SpeedSettings = {
    NormalSpeed = 50,
    StealSpeed = 28,
}

local function applyModeSpeeds(mode)
    local saved = CustomModeSpeeds[mode]
    if saved then
        SpeedSettings.NormalSpeed = saved.NormalSpeed
        SpeedSettings.StealSpeed = saved.StealSpeed
    else
        local defaults = ModeDefaults[mode]
        if defaults then
            SpeedSettings.NormalSpeed = defaults.NormalSpeed
            SpeedSettings.StealSpeed = defaults.StealSpeed
        end
    end
    _G.NuxusBooster.NormalSpeed = SpeedSettings.NormalSpeed
    _G.NuxusBooster.StealSpeed = SpeedSettings.StealSpeed
end

local function saveCurrentModeSpeeds()
    local mode = _G.NuxusBooster.Mode or "NORMAL"
    CustomModeSpeeds[mode] = {
        NormalSpeed = SpeedSettings.NormalSpeed,
        StealSpeed = SpeedSettings.StealSpeed,
    }
    _G.NuxusBooster.CustomModeSpeeds = CustomModeSpeeds
end

applyModeSpeeds(_G.NuxusBooster.Mode)

-- Path Speed Settings
local PathModeDefaults = {
    NORMAL = { PathNormalSpeed = 59, PathStealSpeed = 28 },
    DESYNC = { PathNormalSpeed = 40, PathStealSpeed = 18 },
    LAGGER = { PathNormalSpeed = 35, PathStealSpeed = 14 },
}

local CustomPathModeSpeeds = _G.NuxusBooster.CustomPathModeSpeeds or {}

local PathSpeedSettings = {
    NormalSpeed = 59,
    StealSpeed = 28,
}

local function applyPathModeSpeeds(mode)
    local saved = CustomPathModeSpeeds[mode]
    if saved then
        PathSpeedSettings.NormalSpeed = saved.PathNormalSpeed
        PathSpeedSettings.StealSpeed = saved.PathStealSpeed
    else
        local defaults = PathModeDefaults[mode]
        if defaults then
            PathSpeedSettings.NormalSpeed = defaults.PathNormalSpeed
            PathSpeedSettings.StealSpeed = defaults.PathStealSpeed
        end
    end
    _G.NuxusBooster.PathNormalSpeed = PathSpeedSettings.NormalSpeed
    _G.NuxusBooster.PathStealSpeed = PathSpeedSettings.StealSpeed
end

local function saveCurrentPathModeSpeeds()
    local mode = _G.NuxusBooster.Mode or "NORMAL"
    CustomPathModeSpeeds[mode] = {
        PathNormalSpeed = PathSpeedSettings.NormalSpeed,
        PathStealSpeed = PathSpeedSettings.StealSpeed,
    }
    _G.NuxusBooster.CustomPathModeSpeeds = CustomPathModeSpeeds
end

applyPathModeSpeeds(_G.NuxusBooster.Mode)

-- ═══════════════════════════════════════════════
-- AUTO PATH SYSTEM
-- ═══════════════════════════════════════════════

local currentPathSide = nil
local lastFlatVel = Vector3.zero
local PATH_BASE_STOP = 1.35
local PATH_MIN_STOP = 0.65
local PATH_NEXT_POINT_BIAS = 0.45
local PATH_SMOOTH_FACTOR = 0.12
local pathRunLoopId = 0

local stealPathLeft = {
    {pos = Vector3.new(-475.02, -7.00, 91.73)},
    {pos = Vector3.new(-484.26, -5.04, 97.27)},
    {pos = Vector3.new(-474.89, -7.00, 97.54)},
    {pos = Vector3.new(-475.56, -7.00, 24.56)},
}

local stealPathRight = {
    {pos = Vector3.new(-474.49, -7.00, 28.78)},
    {pos = Vector3.new(-484.39, -4.94, 23.53)},
    {pos = Vector3.new(-475.84, -7.00, 24.65)},
    {pos = Vector3.new(-474.81, -7.00, 101.78)},
}

local function pathMoveToPoint(hrp, targetPos, nextPos, isSteal, myPathId)
    local char = hrp.Parent
    local hum = char and char:FindFirstChildOfClass("Humanoid")
    local conn = nil
    local reached = false

    conn = Services.RunService.Heartbeat:Connect(function()
        if not pathActive or reached or pathRunLoopId ~= myPathId then
            if conn then conn:Disconnect() end
            if hum then hum:Move(Vector3.zero, false) end
            return
        end
        if not hrp or not hrp.Parent then
            if conn then conn:Disconnect() end
            return
        end
        if hum and hum:GetState() == Enum.HumanoidStateType.Physics then return end

        local speed = isSteal and PathSpeedSettings.StealSpeed or PathSpeedSettings.NormalSpeed
        local pos = hrp.Position
        local target = Vector3.new(targetPos.X, pos.Y, targetPos.Z)
        local dir = target - pos
        local dist = dir.Magnitude
        local stopDist = math.clamp(PATH_BASE_STOP - dist * 0.04, PATH_MIN_STOP, PATH_BASE_STOP)

        if dist <= stopDist then
            reached = true
            if conn then conn:Disconnect() end
            if hum then hum:Move(Vector3.zero, false) end
            return
        end

        local moveDir = dir.Unit
        if nextPos then
            local nextDir = (Vector3.new(nextPos.X, pos.Y, nextPos.Z) - pos).Unit
            moveDir = (moveDir + nextDir * PATH_NEXT_POINT_BIAS).Unit
        end
        if lastFlatVel.Magnitude > 0.1 then
            moveDir = (moveDir * (1 - PATH_SMOOTH_FACTOR) + lastFlatVel.Unit * PATH_SMOOTH_FACTOR).Unit
        end

        if hum then hum:Move(moveDir, false) end
        local vel = Vector3.new(moveDir.X * speed, hrp.Velocity.Y, moveDir.Z * speed)
        hrp.Velocity = vel
        lastFlatVel = Vector3.new(vel.X, 0, vel.Z)
    end)

    while pathActive and hrp and hrp.Parent and not reached and pathRunLoopId == myPathId do
        Services.RunService.Heartbeat:Wait()
    end

    if conn and conn.Connected then
        conn:Disconnect()
    end
end

local function runStealPath(path)
    local myPathId = pathRunLoopId
    local char = Player.Character
    if not char then return end
    local hrpLocal = char:FindFirstChild("HumanoidRootPart")
    if not hrpLocal then return end
    for i, p in ipairs(path) do
        if not pathActive or pathRunLoopId ~= myPathId then return end
        local nextP = path[i + 1] and path[i + 1].pos
        pathMoveToPoint(hrpLocal, p.pos, nextP, i > 2, myPathId)
        if not pathActive or pathRunLoopId ~= myPathId then return end
        task.wait()
    end
end

local function startStealPath(path)
    pathActive = true
    local myId = pathRunLoopId + 1
    pathRunLoopId = myId
    task.spawn(function()
        while pathActive and pathRunLoopId == myId do
            runStealPath(path)
            task.wait(0.1)
        end
    end)
end

local function stopStealPath()
    pathActive = false
    pathRunLoopId = pathRunLoopId + 1
    local char = Player.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if hrp then hrp.Velocity = Vector3.zero end
    local hum = char and char:FindFirstChildOfClass("Humanoid")
    if hum then hum:Move(Vector3.zero, false) end
    lastFlatVel = Vector3.zero
end

-- ═══════════════════════════════════════════════
-- AUTO STEAL SYSTEM
-- ═══════════════════════════════════════════════

local StealSettings = {
    Radius = 10,
    Duration = 0.2,
    Cache = {},
    Prompts = {},
}

local ProgressBarFill = nil
local StealRunning = {}
local IsStealing = false
local InternalStealCache = {}

local function scanPlots()
    local plotsFolder = workspace:FindFirstChild("Plots")
    if not plotsFolder then
        StealSettings.Cache = {}
        return
    end
    local newCache = {}
    for _, plot in ipairs(plotsFolder:GetChildren()) do
        if not plot:IsA("Model") then continue end
        local pods = plot:FindFirstChild("AnimalPodiums")
        if not pods then continue end
        for _, pod in ipairs(pods:GetChildren()) do
            if not pod:IsA("Model") then continue end
            local base = pod:FindFirstChild("Base")
            if not base then continue end
            local spawnObj = base:FindFirstChild("Spawn")
            if not spawnObj then continue end
            local attach = spawnObj:FindFirstChild("PromptAttachment")
            if not attach then continue end
            for _, v in ipairs(attach:GetChildren()) do
                if v:IsA("ProximityPrompt") and v.Enabled then
                    local pivot = pod:GetPivot()
                    local uid = plot.Name .. "_" .. pod.Name
                    table.insert(newCache, {
                        plot = plot.Name,
                        slot = pod.Name,
                        pos = pivot.Position,
                        uid = uid,
                        promptObj = v,
                    })
                    StealSettings.Prompts[uid] = v
                end
            end
        end
    end
    StealSettings.Cache = newCache
end

local function getNearest()
    if not isAlive() then return nil end
    local root = getHRP()
    if not root then return nil end
    local nearest = nil
    local minDist = StealSettings.Radius
    local myPos = root.Position
    for _, data in ipairs(StealSettings.Cache) do
        local dist = (myPos - data.pos).Magnitude
        if dist < minDist then
            minDist = dist
            nearest = data
        end
    end
    return nearest
end

local function getPrompt(data)
    if not data then return nil end
    local cached = StealSettings.Prompts[data.uid]
    if cached and cached.Parent and cached.Enabled then return cached end
    local plotsFolder = workspace:FindFirstChild("Plots")
    if not plotsFolder then return nil end
    local plot = plotsFolder:FindFirstChild(data.plot)
    if not plot then return nil end
    local pods = plot:FindFirstChild("AnimalPodiums")
    if not pods then return nil end
    local pod = pods:FindFirstChild(data.slot)
    if not pod then return nil end
    local base = pod:FindFirstChild("Base")
    if not base then return nil end
    local spawnObj = base:FindFirstChild("Spawn")
    if not spawnObj then return nil end
    local attach = spawnObj:FindFirstChild("PromptAttachment")
    if not attach then return nil end
    for _, v in ipairs(attach:GetChildren()) do
        if v:IsA("ProximityPrompt") and v.Enabled then
            StealSettings.Prompts[data.uid] = v
            return v
        end
    end
    return nil
end

local function buildStealCallbacks(prompt)
    if InternalStealCache[prompt] then return end
    local data = { holdCallbacks = {}, triggerCallbacks = {}, ready = true }
    local ok1, conns1 = pcall(getconnections, prompt.PromptButtonHoldBegan)
    if ok1 and type(conns1) == "table" then
        for _, conn in ipairs(conns1) do
            if type(conn.Function) == "function" then
                table.insert(data.holdCallbacks, conn.Function)
            end
        end
    end
    local ok2, conns2 = pcall(getconnections, prompt.Triggered)
    if ok2 and type(conns2) == "table" then
        for _, conn in ipairs(conns2) do
            if type(conn.Function) == "function" then
                table.insert(data.triggerCallbacks, conn.Function)
            end
        end
    end
    if #data.holdCallbacks > 0 or #data.triggerCallbacks > 0 then
        InternalStealCache[prompt] = data
    end
end

local function executeStealInstant(prompt)
    if not prompt or not prompt.Parent then return end
    if IsStealing then return end

    buildStealCallbacks(prompt)

    local cached = InternalStealCache[prompt]
    if not cached then
        pcall(function()
            if fireproximityprompt then
                fireproximityprompt(prompt, 0)
            end
        end)
        return
    end

    if not cached.ready then return end

    IsStealing = true
    cached.ready = false

    task.spawn(function()
        pcall(function()
            if #cached.holdCallbacks > 0 then
                for _, fn in ipairs(cached.holdCallbacks) do
                    task.spawn(fn)
                end
            end

            local startTime = tick()
            local duration = StealSettings.Duration
            while tick() - startTime < duration do
                if ProgressBarFill and ProgressBarFill.Parent then
                    local prog = (tick() - startTime) / duration
                    Services.TweenService:Create(ProgressBarFill, TweenInfo.new(0.03), {
                        Size = UDim2.new(math.clamp(prog, 0, 1), 0, 1, 0)
                    }):Play()
                end
                task.wait(0.03)
            end

            if #cached.triggerCallbacks > 0 then
                for _, fn in ipairs(cached.triggerCallbacks) do
                    task.spawn(fn)
                end
            end

            pcall(function()
                if fireproximityprompt then
                    fireproximityprompt(prompt, 0)
                end
            end)

            task.wait(0.03)
            if ProgressBarFill and ProgressBarFill.Parent then
                Services.TweenService:Create(ProgressBarFill, TweenInfo.new(0.08), {
                    Size = UDim2.new(0, 0, 1, 0)
                }):Play()
            end
        end)

        task.wait(0.08)
        cached.ready = true
        IsStealing = false
    end)
end

-- ═══════════════════════════════════════════════
-- ANTI RAGDOLL
-- ═══════════════════════════════════════════════

local function setupAntiRagdollForChar()
    if not AntiRagdollEnabled then return end
    for _, c in pairs(ARConns) do
        pcall(function() c:Disconnect() end)
    end
    ARConns = {}

    local char = Player.Character
    if not char then return end
    local humanoid = char:WaitForChild("Humanoid")
    local root = char:WaitForChild("HumanoidRootPart")
    local animator = humanoid:WaitForChild("Animator")

    pcall(function() humanoid:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, false) end)
    pcall(function() humanoid:SetStateEnabled(Enum.HumanoidStateType.FallingDown, false) end)
    pcall(function() humanoid:SetStateEnabled(Enum.HumanoidStateType.GettingUp, false) end)

    local recovering = false
    local lastVelocity = Vector3.zero

    local BAD_STATES = {
        [Enum.HumanoidStateType.Physics] = true,
        [Enum.HumanoidStateType.Ragdoll] = true,
        [Enum.HumanoidStateType.FallingDown] = true,
        [Enum.HumanoidStateType.GettingUp] = true,
    }

    local function cleanConstraints()
        if not char or not char.Parent then return end
        for _, obj in pairs(char:GetDescendants()) do
            if obj:IsA("BallSocketConstraint") or obj:IsA("NoCollisionConstraint") or obj:IsA("HingeConstraint") then
                obj:Destroy()
            elseif obj:IsA("Attachment") and (obj.Name == "A" or obj.Name == "B") then
                obj:Destroy()
            elseif obj:IsA("BodyVelocity") or obj:IsA("BodyPosition") or obj:IsA("BodyGyro") then
                obj:Destroy()
            elseif obj:IsA("Motor6D") then
                obj.Enabled = true
            end
        end
        if animator and animator.Parent then
            for _, track in pairs(animator:GetPlayingAnimationTracks()) do
                local name = (track.Animation and track.Animation.Name or ""):lower()
                if name:find("rag") or name:find("fall") or name:find("hurt") or name:find("down") then
                    track:Stop(0)
                end
            end
        end
    end

    local function reEnableControls()
        pcall(function()
            require(Player:WaitForChild("PlayerScripts"):WaitForChild("PlayerModule")):GetControls():Enable()
        end)
    end

    local function startRecovery()
        if recovering then return end
        recovering = true
        task.spawn(function()
            local endTime = tick() + 0.5
            while tick() < endTime do
                if not AntiRagdollEnabled then break end
                if not humanoid or not humanoid.Parent or not root or not root.Parent then break end
                if BAD_STATES[humanoid:GetState()] then
                    cleanConstraints()
                    pcall(function() humanoid:ChangeState(Enum.HumanoidStateType.Running) end)
                    local vel = root.Velocity
                    if vel.Magnitude > 30 then
                        root.Velocity = Vector3.new(0, math.min(vel.Y, 5), 0)
                    end
                    workspace.CurrentCamera.CameraSubject = humanoid
                    reEnableControls()
                end
                pcall(function() humanoid:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, false) end)
                pcall(function() humanoid:SetStateEnabled(Enum.HumanoidStateType.FallingDown, false) end)
                task.wait(0.02)
            end
            recovering = false
        end)
    end

    table.insert(ARConns, humanoid.StateChanged:Connect(function(_, newState)
        if BAD_STATES[newState] then startRecovery() end
    end))

    table.insert(ARConns, Services.RunService.Heartbeat:Connect(function()
        if not AntiRagdollEnabled then return end
        if not humanoid or not humanoid.Parent or not root or not root.Parent then return end
        if BAD_STATES[humanoid:GetState()] then
            startRecovery()
            local vel = root.Velocity
            local velDif = (vel - lastVelocity).Magnitude
            if velDif > 50 and vel.Magnitude > 20 then
                root.Velocity = Vector3.new(0, math.min(vel.Y, 5), 0)
            end
        end
        lastVelocity = root.Velocity
    end))

    table.insert(ARConns, char.DescendantAdded:Connect(function(obj)
        if obj:IsA("BallSocketConstraint") or obj:IsA("NoCollisionConstraint") or obj:IsA("HingeConstraint") or obj:IsA("BodyVelocity") or obj:IsA("BodyPosition") or obj:IsA("BodyGyro") then
            task.defer(function()
                if obj and obj.Parent then obj:Destroy() end
            end)
        elseif obj:IsA("Motor6D") then
            task.defer(function()
                if obj and obj.Parent then obj.Enabled = true end
            end)
        end
    end))

    reEnableControls()
    cleanConstraints()
end

local function enableAntiRagdoll()
    if AntiRagdollEnabled then return end
    AntiRagdollEnabled = true
    setupAntiRagdollForChar()
end

local function disableAntiRagdoll()
    AntiRagdollEnabled = false
    for _, c in pairs(ARConns) do
        pcall(function() c:Disconnect() end)
    end
    ARConns = {}
end

Player.CharacterAdded:Connect(function()
    task.wait(1)
    if AntiRagdollEnabled then
        setupAntiRagdollForChar()
    end
end)

-- ═══════════════════════════════════════════════
-- DROP BRAINROT
-- ═══════════════════════════════════════════════

local function pulseDropBrainrot()
    if DropActive then return end
    DropActive = true

    table.insert(DropConns, Services.RunService.Stepped:Connect(function()
        if not DropActive then return end
        for _, p in ipairs(Services.Players:GetPlayers()) do
            if p ~= Player and p.Character then
                for _, a in ipairs(p.Character:GetChildren()) do
                    if a:IsA("BasePart") then a.CanCollide = false end
                end
            end
        end
    end))

    task.spawn(function()
        while DropActive do
            Services.RunService.Heartbeat:Wait()
            local hrp = getHRP()
            if hrp and hrp.Parent then
                local vel = hrp.Velocity
                hrp.Velocity = vel * 10000 + Vector3.new(0, 10000, 0)
                Services.RunService.RenderStepped:Wait()
                if hrp.Parent then hrp.Velocity = vel end
                Services.RunService.Stepped:Wait()
                if hrp.Parent then hrp.Velocity = vel + Vector3.new(0, 0.1, 0) end
            end
        end
    end)

    task.delay(0.4, function()
        DropActive = false
        for _, c in ipairs(DropConns) do
            pcall(function()
                if typeof(c) == "RBXScriptConnection" then c:Disconnect() end
            end)
        end
        DropConns = {}
    end)
end

-- ═══════════════════════════════════════════════
-- INFINITE JUMP
-- ═══════════════════════════════════════════════

local jumpPower = 44
local jumpActive = false

local function activateJump()
    if jumpConnection then return end
    jumpConnection = Services.UserInputService.JumpRequest:Connect(function()
        local char = Player.Character
        if char then
            local rootPart = char:FindFirstChild("HumanoidRootPart")
            if rootPart then
                rootPart.Velocity = Vector3.new(rootPart.Velocity.X, jumpPower, rootPart.Velocity.Z)
            end
        end
    end)
    jumpActive = true
end

local function deactivateJump()
    if jumpConnection then
        jumpConnection:Disconnect()
        jumpConnection = nil
    end
    jumpActive = false
end

-- ═══════════════════════════════════════════════
-- UNWALK
-- ═══════════════════════════════════════════════

local savedAnimations = {}

local function startUnwalk()
    UnwalkActive = true
    local c = Player.Character
    if not c then return end
    local hum = c:FindFirstChildOfClass("Humanoid")
    if hum then
        for _, t in ipairs(hum:GetPlayingAnimationTracks()) do
            t:Stop()
        end
    end
    local anim = c:FindFirstChild("Animate")
    if anim then
        savedAnimations.Animate = anim:Clone()
        anim:Destroy()
    end
end

local function stopUnwalk()
    UnwalkActive = false
    local c = Player.Character
    if c and savedAnimations.Animate then
        savedAnimations.Animate:Clone().Parent = c
        savedAnimations.Animate = nil
    end
end

Player.CharacterAdded:Connect(function()
    task.wait(1)
    if UnwalkActive then
        task.wait(0.5)
        startUnwalk()
    end
    if SpeedBoostActive then
        local hum = getHumanoid()
        if hum then
            savedWalkSpeed = 16
            hum.WalkSpeed = SpeedSettings.NormalSpeed
        end
    end
end)

-- ═══════════════════════════════════════════════
-- LOCK SYSTEM
-- ═══════════════════════════════════════════════

local LockBtn_Ref = nil

local function findNearestPlayer()
    local c = Player.Character
    if not c then return nil end
    local h = c:FindFirstChild("HumanoidRootPart")
    if not h then return nil end
    local pos = h.Position
    local nearest, dist = nil, math.huge
    for _, p in ipairs(Services.Players:GetPlayers()) do
        if p ~= Player and p.Character then
            local oh = p.Character:FindFirstChild("HumanoidRootPart")
            if oh then
                local d = (pos - oh.Position).Magnitude
                if d < dist then
                    dist = d
                    nearest = p
                end
            end
        end
    end
    return nearest
end

local function startLock()
    if LockConn then return end
    LockConn = Services.RunService.Heartbeat:Connect(function()
        if not LockActive then return end
        local c = Player.Character
        if not c then return end
        local h = c:FindFirstChild("HumanoidRootPart")
        local hum = c:FindFirstChildOfClass("Humanoid")
        if not h or not hum then return end
        local target = findNearestPlayer()
        if not target or not target.Character then
            h.Velocity = Vector3.new(0, h.Velocity.Y, 0)
            return
        end
        local eh = target.Character:FindFirstChild("HumanoidRootPart")
        if not eh then return end
        local pos = h.Position
        local targetPos = eh.Position
        local dir = targetPos - pos
        local flatDir = Vector3.new(dir.X, 0, dir.Z)
        local flatDist = flatDir.Magnitude
        local spd = 55
        if flatDist > 3 then
            local moveDir = flatDir.Unit
            h.Velocity = Vector3.new(moveDir.X * spd, h.Velocity.Y, moveDir.Z * spd)
        elseif flatDist > 0.5 then
            local moveDir = flatDir.Unit
            h.Velocity = Vector3.new(moveDir.X * spd * 0.3, h.Velocity.Y, moveDir.Z * spd * 0.3)
        else
            h.Velocity = Vector3.new(eh.Velocity.X * 0.5, h.Velocity.Y, eh.Velocity.Z * 0.5)
        end
        local yDiff = targetPos.Y - pos.Y
        if math.abs(yDiff) > 1 then
            local yVel = math.clamp(yDiff * 3, -40, 40)
            h.Velocity = Vector3.new(h.Velocity.X, yVel, h.Velocity.Z)
        end
    end)
end

local function stopLock()
    if LockConn then
        LockConn:Disconnect()
        LockConn = nil
    end
    local hrp = getHRP()
    if hrp then
        hrp.Velocity = Vector3.new(0, hrp.Velocity.Y, 0)
    end
end

-- ═══════════════════════════════════════════════
-- SPEED BOOST
-- ═══════════════════════════════════════════════

local function toggleSpeedBoost()
    SpeedBoostActive = not SpeedBoostActive
    if SpeedBoostActive then
        local hum = getHumanoid()
        if hum then
            savedWalkSpeed = hum.WalkSpeed
            hum.WalkSpeed = SpeedSettings.NormalSpeed
        end
        SpeedBoostConn = Services.RunService.Heartbeat:Connect(function()
            if not SpeedBoostActive then return end
            if LockActive then return end
            if pathActive then return end
            local hum = getHumanoid()
            local hrp = getHRP()
            if hum and hrp and hum.MoveDirection.Magnitude > 0 then
                local moveDir = hum.MoveDirection
                local isStealing = Player:GetAttribute("Stealing") == true
                local speed = isStealing and SpeedSettings.StealSpeed or SpeedSettings.NormalSpeed
                hrp.Velocity = Vector3.new(
                    moveDir.X * speed,
                    hrp.Velocity.Y,
                    moveDir.Z * speed
                )
            end
        end)
    else
        local hum = getHumanoid()
        if hum then
            hum.WalkSpeed = savedWalkSpeed
        end
        if SpeedBoostConn then
            SpeedBoostConn:Disconnect()
            SpeedBoostConn = nil
        end
    end
end

-- ═══════════════════════════════════════════════
-- VISUAL RADIUS
-- ═══════════════════════════════════════════════

local function createRadiusBall()
    if RadiusBall then pcall(function() RadiusBall:Destroy() end) end
    RadiusBall = nil
    local hrp = getHRP()
    if not hrp then return end
    RadiusBall = Instance.new("Part")
    RadiusBall.Name = rotateName(NamePool.Part, "RadiusBall")
    RadiusBall.Shape = Enum.PartType.Ball
    local diameter = StealSettings.Radius * 2
    RadiusBall.Size = Vector3.new(diameter, diameter, diameter)
    RadiusBall.Color = TH.red
    RadiusBall.Material = Enum.Material.Neon
    RadiusBall.Transparency = 0.85
    RadiusBall.Anchored = true
    RadiusBall.CanCollide = false
    RadiusBall.CastShadow = false
    RadiusBall.Parent = workspace
    RadiusBall.CFrame = hrp.CFrame
end

local function updateRadiusBallSize()
    if not RadiusBall or not RadiusBall.Parent then return end
    local diameter = StealSettings.Radius * 2
    RadiusBall.Size = Vector3.new(diameter, diameter, diameter)
end

local function updateRadiusBall()
    if not RadiusBall or not RadiusBall.Parent then return end
    local hrp = getHRP()
    if not hrp then return end
    RadiusBall.CFrame = CFrame.new(hrp.Position)
    RadiusBall.Color = TH.red
end

-- ═══════════════════════════════════════════════
-- NAME ROTATION (Anti-detection)
-- ═══════════════════════════════════════════════

task.spawn(function()
    while scriptAlive do
        task.wait(20 + math.random(15))
        pcall(function()
            if GuiRef and GuiRef.Parent then
                GuiRef.Name = rotateName(NamePool.Gui, "Gui")
                for _, child in ipairs(GuiRef:GetChildren()) do
                    if child:IsA("Frame") or child:IsA("TextLabel") or child:IsA("TextButton") then
                        if child.Name ~= "BG" then
                            child.Name = rotateName(NamePool.Frame, "F_" .. math.random(100, 999))
                        end
                    end
                end
            end
            if RadiusBall and RadiusBall.Parent then
                RadiusBall.Name = rotateName(NamePool.Part, "P_" .. math.random(100, 999))
            end
        end)
    end
end)

task.spawn(function()
    while scriptAlive do
        task.wait(10 + math.random(10))
        pcall(function()
            for i = 1, 2 do
                local fake = Instance.new("Frame", Services.CoreGui)
                fake.Name = "Roblox" .. (math.random(1,3) == 1 and "Gui" or "Prompt") .. "_" .. math.random(1000, 9999)
                fake.Size = UDim2.new(0, 0, 0, 0)
                pcall(function() fake.Visible = false end)
                Services.Debris:AddItem(fake, 15 + math.random(15))
            end
        end)
    end
end)

-- Hook protection
local hookSuccess = false
pcall(function()
    if type(getrawmetatable) ~= "function" then return end
    if type(newcclosure) ~= "function" then return end
    if type(getnamecallmethod) ~= "function" then return end
    if type(setreadonly) ~= "function" then return end

    local mt = getrawmetatable(game)
    if not mt then return end
    local oldNC = mt.__namecall
    if type(oldNC) ~= "function" then return end

    setreadonly(mt, false)

    mt.__namecall = newcclosure(function(self, ...)
        local args = {...}
        local method = getnamecallmethod()

        if method == "FindFirstChildOfClass" and self == Services.CoreGui then
            local arg = args[1]
            if type(arg) == "string" and arg == "ScreenGui" then
                local res = oldNC(self, unpack(args))
                if res == GuiRef then return nil end
                return res
            end
        end

        if method == "GetChildren" and self == Services.CoreGui then
            local res = oldNC(self, unpack(args))
            if type(res) == "table" then
                local filtered = {}
                for _, v in ipairs(res) do
                    if v ~= GuiRef then
                        table.insert(filtered, v)
                    end
                end
                return filtered
            end
            return res
        end

        return oldNC(self, unpack(args))
    end)

    setreadonly(mt, true)
    hookSuccess = true
end)

if not hookSuccess then
    pcall(function()
        task.spawn(function()
            while scriptAlive do
                task.wait(5)
                pcall(function()
                    if GuiRef and GuiRef.Parent then
                        GuiRef.Name = rotateName(NamePool.Gui, "Gui")
                    end
                end)
            end
        end)
    end)
end

-- ═══════════════════════════════════════════════
-- MAIN GUI
-- ═══════════════════════════════════════════════

local Gui = Instance.new("ScreenGui")
Gui.Name = rotateName(NamePool.Gui, "Gui")
Gui.ResetOnSpawn = false
Gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
Gui.Parent = Services.CoreGui
GuiRef = Gui

-- Hub Frame
local HubFrameW = 340
local HubFrameH = 24

local FrameHub = Instance.new("Frame", Gui)
FrameHub.Name = rotateName(NamePool.Frame, "Hub")
FrameHub.Size = UDim2.new(0, HubFrameW * Scale, 0, HubFrameH * Scale)
FrameHub.Position = UDim2.new(0.5, -(HubFrameW * Scale) / 2, 0, 8 * Scale)
FrameHub.BackgroundColor3 = TH.bg
FrameHub.Active = true
FrameHub.BackgroundTransparency = 0.7
FrameHub.BorderSizePixel = 0
makeCorner(FrameHub, 6)
createAnimatedBorder(FrameHub, 1.5, 1)

local HubTitle = Instance.new("TextLabel", FrameHub)
HubTitle.Size = UDim2.new(0, 65 * Scale, 1, 0)
HubTitle.Position = UDim2.new(0, 8 * Scale, 0, 0)
HubTitle.BackgroundTransparency = 1
HubTitle.Text = "Nuxus Hub"
HubTitle.Font = Enum.Font.GothamBold
HubTitle.TextSize = 9 * Scale
HubTitle.TextColor3 = TH.redBright
HubTitle.TextXAlignment = Enum.TextXAlignment.Left
createAnimatedTextGradient(HubTitle, 2)

local PingLbl = Instance.new("TextLabel", FrameHub)
PingLbl.Size = UDim2.new(0, 55 * Scale, 1, 0)
PingLbl.Position = UDim2.new(0, 78 * Scale, 0, 0)
PingLbl.BackgroundTransparency = 1
PingLbl.Text = "Ping: --"
PingLbl.Font = Enum.Font.GothamBold
PingLbl.TextSize = 9 * Scale
PingLbl.TextColor3 = TH.redBright
PingLbl.TextXAlignment = Enum.TextXAlignment.Left
createAnimatedTextGradient(PingLbl, 1.5)

local FpsLbl = Instance.new("TextLabel", FrameHub)
FpsLbl.Size = UDim2.new(0, 55 * Scale, 1, 0)
FpsLbl.Position = UDim2.new(0, 136 * Scale, 0, 0)
FpsLbl.BackgroundTransparency = 1
FpsLbl.Text = "FPS: --"
FpsLbl.Font = Enum.Font.GothamBold
FpsLbl.TextSize = 9 * Scale
FpsLbl.TextColor3 = TH.redBright
FpsLbl.TextXAlignment = Enum.TextXAlignment.Left
createAnimatedTextGradient(FpsLbl, 1.5)

-- FPS Detection
local FPS_FrameCount = 0
local FPS_LastTime = tick()
local FPS_Current = 60

FPSConn = Services.RunService.RenderStepped:Connect(function()
    if not FpsLbl or not FpsLbl.Parent then return end
    FPS_FrameCount = FPS_FrameCount + 1
    local currentTime = tick()
    local elapsed = currentTime - FPS_LastTime
    if elapsed >= 1 then
        FPS_Current = math.floor(FPS_FrameCount / elapsed)
        FpsLbl.Text = "FPS: " .. tostring(FPS_Current)
        FPS_FrameCount = 0
        FPS_LastTime = currentTime
    end
end)

-- Ping Detection
task.spawn(function()
    while Gui and Gui.Parent do
        local success, pingValue = pcall(function()
            local perfStats = Services.Stats:FindFirstChild("PerformanceStats")
            if perfStats then
                local pingStat = perfStats:FindFirstChild("Ping")
                if pingStat then
                    return tonumber(pingStat:GetValue()) or 0
                end
            end
            return 0
        end)
        if success and pingValue then
            pingValue = math.floor(pingValue)
            PingLbl.Text = "Ping: " .. tostring(pingValue)
        else
            PingLbl.Text = "Ping: --"
        end
        task.wait(1)
    end
end)

-- Close Button
local CloseAllBtn = Instance.new("TextButton", FrameHub)
CloseAllBtn.Size = UDim2.new(0, 18 * Scale, 0, 18 * Scale)
CloseAllBtn.Position = UDim2.new(1, -22 * Scale, 0.5, -9 * Scale)
CloseAllBtn.BackgroundTransparency = 1
CloseAllBtn.BorderSizePixel = 0
CloseAllBtn.Text = "X"
CloseAllBtn.Font = Enum.Font.GothamBold
CloseAllBtn.TextSize = 10 * Scale
CloseAllBtn.TextColor3 = TH.redBright
CloseAllBtn.ZIndex = 10
CloseAllBtn.AutoButtonColor = false
createAnimatedBorder(CloseAllBtn, 1.5, 1)
createAnimatedTextGradient(CloseAllBtn, 2)

local CloseBg = Instance.new("Frame", CloseAllBtn)
CloseBg.Name = "BG"
CloseBg.Size = UDim2.new(1, 0, 1, 0)
CloseBg.BackgroundColor3 = Color3.fromRGB(35, 15, 20)
CloseBg.BackgroundTransparency = 0.3
CloseBg.BorderSizePixel = 0
CloseBg.ZIndex = -1
makeCorner(CloseBg, 3)

-- Trial Timer
local TrialTimerLbl = Instance.new("TextLabel", FrameHub)
TrialTimerLbl.Size = UDim2.new(0, 114 * Scale, 1, 0)
TrialTimerLbl.Position = UDim2.new(1, -138 * Scale, 0, 0)
TrialTimerLbl.BackgroundTransparency = 1
TrialTimerLbl.Text = ""
TrialTimerLbl.Font = Enum.Font.GothamBold
TrialTimerLbl.TextSize = 9 * Scale
TrialTimerLbl.TextColor3 = TH.redBright
TrialTimerLbl.TextXAlignment = Enum.TextXAlignment.Right
TrialTimerLbl.Visible = false

if isTrialUser then
    TrialTimerLbl.Visible = true
    task.spawn(function()
        while trialRemaining > 0 and scriptAlive and TrialTimerLbl and TrialTimerLbl.Parent do
            local mins = math.floor(trialRemaining / 60)
            local secs = trialRemaining % 60
            TrialTimerLbl.Text = string.format("%d:%02d | Key: %s", mins, secs, TRIAL_KEY)
            task.wait(0.5)
        end
        if TrialTimerLbl and TrialTimerLbl.Parent then
            TrialTimerLbl.Text = string.format("0:00 | Key: %s", TRIAL_KEY)
        end
    end)
end

-- ═══════════════════════════════════════════════
-- BUTTON REFERENCES
-- ═══════════════════════════════════════════════

local BtnL_Ref = nil
local BtnR_Ref = nil
local DropBtn_Ref = nil
local HotkeyLabels = {}
local listeningForHotkey = nil

-- ═══════════════════════════════════════════════
-- TOOLS FRAME
-- ═══════════════════════════════════════════════

local toolsFrameW = 185
local toolsFrameH = 155
local toolsPad = 8
local toolsBtnW = toolsFrameW - toolsPad * 2
local toolsBtnH = 30
local toolsGap = 4

local FrameTools = Instance.new("Frame", Gui)
FrameTools.Name = rotateName(NamePool.Frame, "FrameTools")
FrameTools.Size = UDim2.new(0, toolsFrameW * Scale, 0, toolsFrameH * Scale)
FrameTools.Position = UDim2.new(0, 10 * Scale, 0, 42 * Scale)
FrameTools.BackgroundColor3 = TH.bg
FrameTools.BackgroundTransparency = 0.7
FrameTools.BorderSizePixel = 0
FrameTools.ClipsDescendants = true
makeCorner(FrameTools, 10)
createAnimatedBorder(FrameTools, 1.5, 1.5)

local ToolsHeader = Instance.new("Frame", FrameTools)
ToolsHeader.Size = UDim2.new(1, 0, 0, 26 * Scale)
ToolsHeader.BackgroundColor3 = TH.headerBg
ToolsHeader.BackgroundTransparency = 1
ToolsHeader.BorderSizePixel = 0
ToolsHeader.ZIndex = 2
makeCorner(ToolsHeader, 10)

local ToolsTitle = Instance.new("TextLabel", ToolsHeader)
ToolsTitle.Size = UDim2.new(1, -28 * Scale, 1, 0)
ToolsTitle.Position = UDim2.new(0, 8 * Scale, 0, 0)
ToolsTitle.BackgroundTransparency = 1
ToolsTitle.Text = "Tools"
ToolsTitle.Font = Enum.Font.GothamBold
ToolsTitle.TextSize = 11 * Scale
ToolsTitle.TextColor3 = TH.redBright
ToolsTitle.ZIndex = 2
ToolsTitle.TextXAlignment = Enum.TextXAlignment.Left
createAnimatedTextGradient(ToolsTitle, 2)

local ToolsToggleBtn = Instance.new("TextButton", ToolsHeader)
ToolsToggleBtn.Size = UDim2.new(0, 22 * Scale, 0, 22 * Scale)
ToolsToggleBtn.Position = UDim2.new(1, -26 * Scale, 0.5, -11 * Scale)
ToolsToggleBtn.BackgroundTransparency = 1
ToolsToggleBtn.BorderSizePixel = 0
ToolsToggleBtn.Text = "-"
ToolsToggleBtn.Font = Enum.Font.GothamBold
ToolsToggleBtn.TextSize = 14 * Scale
ToolsToggleBtn.TextColor3 = TH.redBright
ToolsToggleBtn.ZIndex = 3
ToolsToggleBtn.AutoButtonColor = false

local ToolsToggleBg = Instance.new("Frame", ToolsToggleBtn)
ToolsToggleBg.Name = "BG"
ToolsToggleBg.Size = UDim2.new(1, 0, 1, 0)
ToolsToggleBg.BackgroundColor3 = TH.rowBg
ToolsToggleBg.BackgroundTransparency = 0.5
ToolsToggleBg.BorderSizePixel = 0
ToolsToggleBg.ZIndex = -1
makeCorner(ToolsToggleBg, 3)

local ToolsContent = Instance.new("Frame", FrameTools)
ToolsContent.Name = "Content"
ToolsContent.Size = UDim2.new(1, -16 * Scale, 1, -(28 * Scale))
ToolsContent.Position = UDim2.new(0, 8 * Scale, 0, 28 * Scale)
ToolsContent.BackgroundTransparency = 1
ToolsContent.ClipsDescendants = true

local ARBtn = makeBtn(ToolsContent, 0, 0, toolsBtnW, toolsBtnH, "Anti Ragdoll: OFF", 11)
local JumpBtn = makeBtn(ToolsContent, 0, (toolsBtnH + toolsGap), toolsBtnW, toolsBtnH, "Infinite Jump: OFF", 11)
local SpeedBoostBtn = makeBtn(ToolsContent, 0, (toolsBtnH + toolsGap) * 2, toolsBtnW, toolsBtnH, "Speed Boost: OFF", 11)
local UnwalkBtn = makeBtn(ToolsContent, 0, (toolsBtnH + toolsGap) * 3, toolsBtnW, toolsBtnH, "Unwalk: OFF", 11)

local toolsCollapsed = false
local toolsOriginalSize = UDim2.new(0, toolsFrameW * Scale, 0, toolsFrameH * Scale)
local toolsCollapsedSize = UDim2.new(0, toolsFrameW * Scale, 0, 28 * Scale)

ToolsToggleBtn.MouseButton1Click:Connect(function()
    toolsCollapsed = not toolsCollapsed
    if toolsCollapsed then
        ToolsContent.Visible = false
        ToolsToggleBtn.Text = "+"
        Services.TweenService:Create(FrameTools, TweenInfo.new(0.15, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Size = toolsCollapsedSize}):Play()
    else
        ToolsContent.Visible = true
        ToolsToggleBtn.Text = "-"
        Services.TweenService:Create(FrameTools, TweenInfo.new(0.15, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Size = toolsOriginalSize}):Play()
    end
end)

makeFrameDraggableByHeader(FrameTools, ToolsHeader)

-- ═══════════════════════════════════════════════
-- DROP BUTTON
-- ═══════════════════════════════════════════════

local DropBtn = Instance.new("TextButton", Gui)
DropBtn.Name = rotateName(NamePool.Frame, "DropBtn")
DropBtn.Size = UDim2.new(0, 110 * Scale, 0, 32 * Scale)
DropBtn.Position = UDim2.new(0, 10 * Scale, 0, (42 + toolsFrameH + 6) * Scale)
DropBtn.BackgroundTransparency = 1
DropBtn.BorderSizePixel = 0
DropBtn.Text = "Drop Brainrot"
DropBtn.TextColor3 = TH.redBright
DropBtn.Font = Enum.Font.GothamBold
DropBtn.TextSize = 11 * Scale
DropBtn.AutoButtonColor = false
DropBtn.Active = true
createAnimatedBorder(DropBtn, 1.5, 1)
createAnimatedTextGradient(DropBtn, 2)

local DropBg = Instance.new("Frame", DropBtn)
DropBg.Name = "BG"
DropBg.Size = UDim2.new(1, 0, 1, 0)
DropBg.BackgroundColor3 = TH.rowBg
DropBg.BackgroundTransparency = 0.5
DropBg.BorderSizePixel = 0
DropBg.ZIndex = -1
makeCorner(DropBg, 8)

DropBtn_Ref = DropBtn

local function doDropBrainrot()
    if DropActive then return end
    DropBtn.Text = "Drop Brainrot..."
    setBtnBg(DropBtn, TH.danger)
    pulseDropBrainrot()
    task.delay(0.5, function()
        if DropBtn and DropBtn.Parent then
            DropBtn.Text = "Drop Brainrot"
            setBtnBg(DropBtn, TH.rowBg)
        end
    end)
end

makeDraggable(DropBtn, doDropBrainrot)

-- ═══════════════════════════════════════════════
-- LOCK BUTTON
-- ═══════════════════════════════════════════════

local LockBtn = Instance.new("TextButton", Gui)
LockBtn.Name = rotateName(NamePool.Frame, "LockBtn")
LockBtn.Size = UDim2.new(0, 110 * Scale, 0, 32 * Scale)
LockBtn.Position = UDim2.new(0, 10 * Scale, 0, (42 + toolsFrameH + 6 + 38) * Scale)
LockBtn.BackgroundTransparency = 1
LockBtn.BorderSizePixel = 0
LockBtn.Text = "Lock"
LockBtn.TextColor3 = TH.redBright
LockBtn.Font = Enum.Font.GothamBold
LockBtn.TextSize = 11 * Scale
LockBtn.AutoButtonColor = false
LockBtn.Active = true
createAnimatedBorder(LockBtn, 1.5, 1)
createAnimatedTextGradient(LockBtn, 2)

local LockBtnBg = Instance.new("Frame", LockBtn)
LockBtnBg.Name = "BG"
LockBtnBg.Size = UDim2.new(1, 0, 1, 0)
LockBtnBg.BackgroundColor3 = TH.rowBg
LockBtnBg.BackgroundTransparency = 0.5
LockBtnBg.BorderSizePixel = 0
LockBtnBg.ZIndex = -1
makeCorner(LockBtnBg, 8)

LockBtn_Ref = LockBtn

local function doToggleLock()
    LockActive = not LockActive
    if LockActive then
        if pathActive then
            stopStealPath()
            currentPathSide = nil
            if BtnL_Ref and BtnL_Ref.Parent then setBtnBg(BtnL_Ref, TH.rowBg) end
            if BtnR_Ref and BtnR_Ref.Parent then setBtnBg(BtnR_Ref, TH.rowBg) end
        end
        startLock()
        LockBtn.Text = "Lock ON"
        setBtnBg(LockBtn, TH.redDark)
    else
        stopLock()
        LockBtn.Text = "Lock"
        setBtnBg(LockBtn, TH.rowBg)
    end
end

makeDraggable(LockBtn, doToggleLock)

-- ═══════════════════════════════════════════════
-- MUSIC BUTTON 2
-- ═══════════════════════════════════════════════

local MusicSound2 = Instance.new("Sound")
MusicSound2.SoundId = "rbxassetid://9125304578"
MusicSound2.Volume = 0.15
MusicSound2.Looped = true
MusicSound2.Parent = SoundParent

local MusicBtn2 = Instance.new("TextButton", Gui)
MusicBtn2.Name = rotateName(NamePool.Frame, "MusicBtn")
MusicBtn2.Size = UDim2.new(0, 42 * Scale, 0, 42 * Scale)
MusicBtn2.Position = UDim2.new(0, 10 * Scale, 0, (42 + toolsFrameH + 6 + 38 + 38) * Scale)
MusicBtn2.BackgroundTransparency = 1
MusicBtn2.BorderSizePixel = 0
MusicBtn2.Text = "♪"
MusicBtn2.TextColor3 = TH.redBright
MusicBtn2.Font = Enum.Font.GothamBold
MusicBtn2.TextSize = 22 * Scale
MusicBtn2.AutoButtonColor = false
MusicBtn2.Active = true
createAnimatedBorder(MusicBtn2, 1.5, 1)
createAnimatedTextGradient(MusicBtn2, 2)

local MusicBtn2Bg = Instance.new("Frame", MusicBtn2)
MusicBtn2Bg.Name = "BG"
MusicBtn2Bg.Size = UDim2.new(1, 0, 1, 0)
MusicBtn2Bg.BackgroundColor3 = TH.rowBg
MusicBtn2Bg.BackgroundTransparency = 0.5
MusicBtn2Bg.BorderSizePixel = 0
MusicBtn2Bg.ZIndex = -1
makeCorner(MusicBtn2Bg, 8)

local musicPlaying2 = false
makeDraggable(MusicBtn2, function()
    musicPlaying2 = not musicPlaying2
    if musicPlaying2 then
        pcall(function() MusicSound2:Play() end)
        MusicBtn2.Text = "♫"
        setBtnBg(MusicBtn2, TH.redDark)
    else
        pcall(function() MusicSound2:Stop() end)
        MusicBtn2.Text = "♪"
        setBtnBg(MusicBtn2, TH.rowBg)
    end
end)

-- ═══════════════════════════════════════════════
-- HOTKEYS FRAME (PC Only)
-- ═══════════════════════════════════════════════

local rightW = 216
local rightPad = 10
local rightGap = 6

if isPC then
    local hkH = 180

    local FrameHotkey = Instance.new("Frame", Gui)
    FrameHotkey.Name = rotateName(NamePool.Frame, "Hotkey")
    FrameHotkey.Size = UDim2.new(0, rightW * Scale, 0, hkH * Scale)
    FrameHotkey.Position = UDim2.new(1, -(rightW + rightPad) * Scale, 0, 42 * Scale)
    FrameHotkey.BackgroundColor3 = TH.bg
    FrameHotkey.BackgroundTransparency = 0.7
    FrameHotkey.BorderSizePixel = 0
    makeCorner(FrameHotkey, 10)
    createAnimatedBorder(FrameHotkey, 1.5, 1.5)
    makeHeader(FrameHotkey, "Hotkeys [PC]")

    local ky0 = 28
    local kGap = 24
    local kBtnW = 60
    local kLabelW = 72

    local LeftKeyLabel = makeStyledLabel(FrameHotkey, 8, ky0, kLabelW, 20, "Auto Left:", 10)
    local LeftKeyBtn = makeBtn(FrameHotkey, 86, ky0, kBtnW, 20, Hotkeys.AutoLeft, 10)

    local RightKeyLabel = makeStyledLabel(FrameHotkey, 8, ky0 + kGap, kLabelW, 20, "Auto Right:", 10)
    local RightKeyBtn = makeBtn(FrameHotkey, 86, ky0 + kGap, kBtnW, 20, Hotkeys.AutoRight, 10)

    local LockKeyLabel = makeStyledLabel(FrameHotkey, 8, ky0 + kGap * 2, kLabelW, 20, "Lock:", 10)
    local LockKeyBtn = makeBtn(FrameHotkey, 86, ky0 + kGap * 2, kBtnW, 20, Hotkeys.Lock, 10)

    local DropKeyLabel = makeStyledLabel(FrameHotkey, 8, ky0 + kGap * 3, kLabelW, 20, "Drop:", 10)
    local DropKeyBtn = makeBtn(FrameHotkey, 86, ky0 + kGap * 3, kBtnW, 20, Hotkeys.Drop, 10)

    local SpeedKeyLabel = makeStyledLabel(FrameHotkey, 8, ky0 + kGap * 4, kLabelW, 20, "Speed:", 10)
    local SpeedKeyBtn = makeBtn(FrameHotkey, 86, ky0 + kGap * 4, kBtnW, 20, Hotkeys.SpeedBoost, 10)

    local ModeKeyLabel = makeStyledLabel(FrameHotkey, 8, ky0 + kGap * 5, kLabelW, 20, "Mode:", 10)
    local ModeKeyBtn = makeBtn(FrameHotkey, 86, ky0 + kGap * 5, kBtnW, 20, Hotkeys.ModeCycle, 10)

    HotkeyLabels = { Left = LeftKeyBtn, Right = RightKeyBtn, Lock = LockKeyBtn, Drop = DropKeyBtn, Speed = SpeedKeyBtn, Mode = ModeKeyBtn }

    local function setupHotkeyButton(btn, keyType)
        btn.MouseButton1Click:Connect(function()
            if listeningForHotkey then return end
            listeningForHotkey = keyType
            btn.Text = "..."
            setBtnBg(btn, TH.redBright)

            local connection
            connection = Services.UserInputService.InputBegan:Connect(function(input, gameProcessed)
                if gameProcessed then return end
                local keyName = input.KeyCode.Name
                if keyName and keyName ~= "Unknown" then
                    Hotkeys[keyType] = keyName
                    btn.Text = keyName
                    setBtnBg(btn, TH.rowBg)
                end
                connection:Disconnect()
                listeningForHotkey = nil
            end)

            task.delay(5, function()
                if listeningForHotkey == keyType then
                    btn.Text = Hotkeys[keyType]
                    setBtnBg(btn, TH.rowBg)
                    listeningForHotkey = nil
                    if connection then connection:Disconnect() end
                end
            end)
        end)
    end

    setupHotkeyButton(LeftKeyBtn, "AutoLeft")
    setupHotkeyButton(RightKeyBtn, "AutoRight")
    setupHotkeyButton(LockKeyBtn, "Lock")
    setupHotkeyButton(DropKeyBtn, "Drop")
    setupHotkeyButton(SpeedKeyBtn, "SpeedBoost")
    setupHotkeyButton(ModeKeyBtn, "ModeCycle")

    makeFrameDraggableByHeader(FrameHotkey, FrameHotkey)

    -- Hotkey Handler
    Services.UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then return end
        if listeningForHotkey then return end
        local keyName = input.KeyCode.Name

        if keyName == Hotkeys.AutoLeft then
            if BtnL_Ref then
                local connections = getconnections and getconnections(BtnL_Ref.MouseButton1Click)
                if connections then
                    for _, conn in pairs(connections) do
                        pcall(function() conn:Fire() end)
                    end
                end
            end
        elseif keyName == Hotkeys.AutoRight then
            if BtnR_Ref then
                local connections = getconnections and getconnections(BtnR_Ref.MouseButton1Click)
                if connections then
                    for _, conn in pairs(connections) do
                        pcall(function() conn:Fire() end)
                    end
                end
            end
        elseif keyName == Hotkeys.Lock then
            doToggleLock()
        elseif keyName == Hotkeys.Drop then
            doDropBrainrot()
        elseif keyName == Hotkeys.SpeedBoost then
            toggleSpeedBoost()
            if SpeedBoostActive then
                SpeedBoostBtn.Text = "Speed Boost: ON"
                setBtnBg(SpeedBoostBtn, TH.redDark)
            else
                SpeedBoostBtn.Text = "Speed Boost: OFF"
                setBtnBg(SpeedBoostBtn, TH.rowBg)
            end
        elseif keyName == Hotkeys.ModeCycle then
            local modes = {"NORMAL", "DESYNC", "LAGGER"}
            local currentIdx = 1
            for i, m in ipairs(modes) do
                if m == (_G.NuxusBooster.Mode or "NORMAL") then
                    currentIdx = i
                    break
                end
            end
            local nextIdx = (currentIdx % #modes) + 1
            updateModeButtons(modes[nextIdx])
        end
    end)
end

-- ═══════════════════════════════════════════════
-- SPEED CONFIG FRAME
-- ═══════════════════════════════════════════════

local spY = isPC and (42 + 180 + rightGap) or 42
local spFrameW = 216
local spFrameH = 250

local FrameSpeed = Instance.new("Frame", Gui)
FrameSpeed.Name = rotateName(NamePool.Frame, "FrameSpeed")
FrameSpeed.Size = UDim2.new(0, spFrameW * Scale, 0, spFrameH * Scale)
FrameSpeed.Position = UDim2.new(1, -(spFrameW + rightPad) * Scale, 0, spY * Scale)
FrameSpeed.BackgroundColor3 = TH.bg
FrameSpeed.BackgroundTransparency = 0.7
FrameSpeed.BorderSizePixel = 0
FrameSpeed.ClipsDescendants = true
makeCorner(FrameSpeed, 10)
createAnimatedBorder(FrameSpeed, 1.5, 1.5)

local SpeedHeader = Instance.new("Frame", FrameSpeed)
SpeedHeader.Size = UDim2.new(1, 0, 0, 22 * Scale)
SpeedHeader.BackgroundColor3 = TH.headerBg
SpeedHeader.BackgroundTransparency = 1
SpeedHeader.BorderSizePixel = 0
SpeedHeader.ZIndex = 2
makeCorner(SpeedHeader, 10)

local SpeedTitle = Instance.new("TextLabel", SpeedHeader)
SpeedTitle.Size = UDim2.new(1, -28 * Scale, 1, 0)
SpeedTitle.Position = UDim2.new(0, 8 * Scale, 0, 0)
SpeedTitle.BackgroundTransparency = 1
SpeedTitle.Text = "Speed Config"
SpeedTitle.Font = Enum.Font.GothamBold
SpeedTitle.TextSize = 11 * Scale
SpeedTitle.TextColor3 = TH.redBright
SpeedTitle.ZIndex = 2
SpeedTitle.TextXAlignment = Enum.TextXAlignment.Left
createAnimatedTextGradient(SpeedTitle, 2)

local SpeedToggleBtn = Instance.new("TextButton", SpeedHeader)
SpeedToggleBtn.Size = UDim2.new(0, 22 * Scale, 0, 22 * Scale)
SpeedToggleBtn.Position = UDim2.new(1, -26 * Scale, 0.5, -11 * Scale)
SpeedToggleBtn.BackgroundTransparency = 1
SpeedToggleBtn.BorderSizePixel = 0
SpeedToggleBtn.Text = "-"
SpeedToggleBtn.Font = Enum.Font.GothamBold
SpeedToggleBtn.TextSize = 14 * Scale
SpeedToggleBtn.TextColor3 = TH.redBright
SpeedToggleBtn.ZIndex = 3
SpeedToggleBtn.AutoButtonColor = false

local SpeedToggleBg = Instance.new("Frame", SpeedToggleBtn)
SpeedToggleBg.Name = "BG"
SpeedToggleBg.Size = UDim2.new(1, 0, 1, 0)
SpeedToggleBg.BackgroundColor3 = TH.rowBg
SpeedToggleBg.BackgroundTransparency = 0.5
SpeedToggleBg.BorderSizePixel = 0
SpeedToggleBg.ZIndex = -1
makeCorner(SpeedToggleBg, 3)

local SpeedContent = Instance.new("Frame", FrameSpeed)
SpeedContent.Name = "Content"
SpeedContent.Size = UDim2.new(1, -16 * Scale, 1, -(24 * Scale))
SpeedContent.Position = UDim2.new(0, 8 * Scale, 0, 24 * Scale)
SpeedContent.BackgroundTransparency = 1
SpeedContent.ClipsDescendants = true

-- Mode Row
local ModeRow = Instance.new("Frame", SpeedContent)
ModeRow.Size = UDim2.new(1, 0, 0, 24 * Scale)
ModeRow.Position = UDim2.new(0, 0, 0, 0)
ModeRow.BackgroundColor3 = TH.rowBg
ModeRow.BackgroundTransparency = 0.5
ModeRow.BorderSizePixel = 0
makeCorner(ModeRow, 5)
createAnimatedBorder(ModeRow, 1.5, 1)

local ModePrefixLbl = makeStyledLabel(ModeRow, 6, 0, 44, 24, "MODE:", 10)

local currentMode = _G.NuxusBooster.Mode or "NORMAL"

local ModeValLbl = Instance.new("TextLabel", ModeRow)
ModeValLbl.Size = UDim2.new(1, -56 * Scale, 1, 0)
ModeValLbl.Position = UDim2.new(0, 48 * Scale, 0, 0)
ModeValLbl.BackgroundTransparency = 1
ModeValLbl.Text = currentMode
ModeValLbl.TextColor3 = TH.redBright
ModeValLbl.Font = Enum.Font.GothamBlack
ModeValLbl.TextSize = 11 * Scale
ModeValLbl.TextXAlignment = Enum.TextXAlignment.Left
createAnimatedTextGradient(ModeValLbl, 2)

-- Mode Buttons
local mbY = 28
local mbW = 66
local mbH = 24
local mbGap = 4
local mbTotalW = 3 * mbW + 2 * mbGap
local mbStartX = (spFrameW - 16 - mbTotalW) / 2

local NormalModeBtn = makeBtn(SpeedContent, mbStartX, mbY, mbW, mbH, "NORMAL", 10)
setBtnBg(NormalModeBtn, (currentMode == "NORMAL") and TH.redDark or TH.rowBg)

local DesyncModeBtn = makeBtn(SpeedContent, mbStartX + mbW + mbGap, mbY, mbW, mbH, "DESYNC", 10)
setBtnBg(DesyncModeBtn, (currentMode == "DESYNC") and TH.red or TH.rowBg)

local LaggerModeBtn = makeBtn(SpeedContent, mbStartX + 2 * (mbW + mbGap), mbY, mbW, mbH, "LAGGER", 10)
setBtnBg(LaggerModeBtn, (currentMode == "LAGGER") and TH.redDark or TH.rowBg)

-- Divider
local DividerLine1 = Instance.new("Frame", SpeedContent)
DividerLine1.Size = UDim2.new(1, 0, 0, 2 * Scale)
DividerLine1.Position = UDim2.new(0, 0, 0, 58 * Scale)
DividerLine1.BackgroundColor3 = TH.redDark
DividerLine1.BackgroundTransparency = 0.5
DividerLine1.BorderSizePixel = 0
makeCorner(DividerLine1, 1)

-- Speed Boost Settings
local SBSLabel = makeStyledLabel(SpeedContent, 0, 64, spFrameW - 16, 18, "Speed Boost", 11)

local sbBoxX = 50
local sbBoxW = 90

local NormalLabel = makeStyledLabel(SpeedContent, 0, 84, 44, 20, "Normal:", 11)
local NormalBox = makeStyledBox(SpeedContent, sbBoxX, 84, sbBoxW, 20, tostring(SpeedSettings.NormalSpeed))

local StealLabel = makeStyledLabel(SpeedContent, 0, 108, 44, 20, "Steal:", 11)
local StealBox = makeStyledBox(SpeedContent, sbBoxX, 108, sbBoxW, 20, tostring(SpeedSettings.StealSpeed))

-- Divider 2
local DividerLine2 = Instance.new("Frame", SpeedContent)
DividerLine2.Size = UDim2.new(1, 0, 0, 2 * Scale)
DividerLine2.Position = UDim2.new(0, 0, 0, 134 * Scale)
DividerLine2.BackgroundColor3 = TH.redDark
DividerLine2.BackgroundTransparency = 0.5
DividerLine2.BorderSizePixel = 0
makeCorner(DividerLine2, 1)

-- Auto Path Settings
local APSLabel = makeStyledLabel(SpeedContent, 0, 140, spFrameW - 16, 18, "Auto Path", 11)

local PathNormalLabel = makeStyledLabel(SpeedContent, 0, 160, 44, 20, "Normal:", 11)
local PathNormalBox = makeStyledBox(SpeedContent, sbBoxX, 160, sbBoxW, 20, tostring(PathSpeedSettings.NormalSpeed))

local PathStealLabel = makeStyledLabel(SpeedContent, 0, 184, 44, 20, "Steal:", 11)
local PathStealBox = makeStyledBox(SpeedContent, sbBoxX, 184, sbBoxW, 20, tostring(PathSpeedSettings.StealSpeed))

-- Box Events
NormalBox.FocusLost:Connect(function()
    local val = tonumber(NormalBox.Text)
    if val and val > 0 and val <= 500 then
        SpeedSettings.NormalSpeed = val
        _G.NuxusBooster.NormalSpeed = val
        saveCurrentModeSpeeds()
    else
        NormalBox.Text = tostring(SpeedSettings.NormalSpeed)
    end
end)

StealBox.FocusLost:Connect(function()
    local val = tonumber(StealBox.Text)
    if val and val > 0 and val <= 500 then
        SpeedSettings.StealSpeed = val
        _G.NuxusBooster.StealSpeed = val
        saveCurrentModeSpeeds()
    else
        StealBox.Text = tostring(SpeedSettings.StealSpeed)
    end
end)

PathNormalBox.FocusLost:Connect(function()
    local val = tonumber(PathNormalBox.Text)
    if val and val > 0 and val <= 500 then
        PathSpeedSettings.NormalSpeed = val
        _G.NuxusBooster.PathNormalSpeed = val
        saveCurrentPathModeSpeeds()
    else
        PathNormalBox.Text = tostring(PathSpeedSettings.NormalSpeed)
    end
end)

PathStealBox.FocusLost:Connect(function()
    local val = tonumber(PathStealBox.Text)
    if val and val > 0 and val <= 500 then
        PathSpeedSettings.StealSpeed = val
        _G.NuxusBooster.PathStealSpeed = val
        saveCurrentPathModeSpeeds()
    else
        PathStealBox.Text = tostring(PathSpeedSettings.StealSpeed)
    end
end)

-- Mode Update Function
local function updateModeButtons(mode)
    mode = mode or "NORMAL"
    saveCurrentModeSpeeds()
    saveCurrentPathModeSpeeds()
    _G.NuxusBooster.Mode = mode
    applyModeSpeeds(mode)
    applyPathModeSpeeds(mode)
    ModeValLbl.Text = mode
    setBtnBg(NormalModeBtn, (mode == "NORMAL") and TH.redDark or TH.rowBg)
    setBtnBg(DesyncModeBtn, (mode == "DESYNC") and TH.red or TH.rowBg)
    setBtnBg(LaggerModeBtn, (mode == "LAGGER") and TH.redDark or TH.rowBg)
    NormalBox.Text = tostring(SpeedSettings.NormalSpeed)
    StealBox.Text = tostring(SpeedSettings.StealSpeed)
    PathNormalBox.Text = tostring(PathSpeedSettings.NormalSpeed)
    PathStealBox.Text = tostring(PathSpeedSettings.StealSpeed)
end

NormalModeBtn.MouseButton1Click:Connect(function() updateModeButtons("NORMAL") end)
DesyncModeBtn.MouseButton1Click:Connect(function() updateModeButtons("DESYNC") end)
LaggerModeBtn.MouseButton1Click:Connect(function() updateModeButtons("LAGGER") end)

-- Collapse Toggle
local speedCollapsed = false
local speedOriginalSize = UDim2.new(0, spFrameW * Scale, 0, spFrameH * Scale)
local speedCollapsedSize = UDim2.new(0, spFrameW * Scale, 0, 26 * Scale)

SpeedToggleBtn.MouseButton1Click:Connect(function()
    speedCollapsed = not speedCollapsed
    if speedCollapsed then
        SpeedContent.Visible = false
        SpeedToggleBtn.Text = "+"
        Services.TweenService:Create(FrameSpeed, TweenInfo.new(0.15, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Size = speedCollapsedSize}):Play()
    else
        SpeedContent.Visible = true
        SpeedToggleBtn.Text = "-"
        Services.TweenService:Create(FrameSpeed, TweenInfo.new(0.15, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Size = speedOriginalSize}):Play()
    end
end)

makeFrameDraggableByHeader(FrameSpeed, SpeedHeader)

-- ═══════════════════════════════════════════════
-- AUTO PATH FRAME
-- ═══════════════════════════════════════════════

local pathFrameY = spY + spFrameH + rightGap
local pathFrameW = 216
local pathFrameH = 60

local FramePath = Instance.new("Frame", Gui)
FramePath.Name = rotateName(NamePool.Frame, "FramePath")
FramePath.Size = UDim2.new(0, pathFrameW * Scale, 0, pathFrameH * Scale)
FramePath.Position = UDim2.new(1, -(pathFrameW + rightPad) * Scale, 0, pathFrameY * Scale)
FramePath.BackgroundColor3 = TH.bg
FramePath.BackgroundTransparency = 0.7
FramePath.BorderSizePixel = 0
makeCorner(FramePath, 10)
createAnimatedBorder(FramePath, 1.5, 1.5)
makeHeader(FramePath, "Auto Path")

local BtnL = makeBtn(FramePath, 15, 28, 85, 26, "LEFT", 12)
local BtnR = makeBtn(FramePath, 120, 28, 85, 26, "RIGHT", 12)

BtnL_Ref = BtnL
BtnR_Ref = BtnR

local function resetPathBtnColors()
    setBtnBg(BtnL, TH.rowBg)
    setBtnBg(BtnR, TH.rowBg)
end

makeFrameDraggableByHeader(FramePath, FramePath)

BtnL.MouseButton1Click:Connect(function()
    if pathActive and currentPathSide == "LEFT" then
        stopStealPath()
        currentPathSide = nil
        resetPathBtnColors()
    else
        if LockActive then
            LockActive = false
            stopLock()
            if LockBtn_Ref and LockBtn_Ref.Parent then
                LockBtn_Ref.Text = "Lock"
                setBtnBg(LockBtn_Ref, TH.rowBg)
            end
        end
        stopStealPath()
        currentPathSide = "LEFT"
        resetPathBtnColors()
        setBtnBg(BtnL, TH.redDark)
        startStealPath(stealPathLeft)
    end
end)

BtnR.MouseButton1Click:Connect(function()
    if pathActive and currentPathSide == "RIGHT" then
        stopStealPath()
        currentPathSide = nil
        resetPathBtnColors()
    else
        if LockActive then
            LockActive = false
            stopLock()
            if LockBtn_Ref and LockBtn_Ref.Parent then
                LockBtn_Ref.Text = "Lock"
                setBtnBg(LockBtn_Ref, TH.rowBg)
            end
        end
        stopStealPath()
        currentPathSide = "RIGHT"
        resetPathBtnColors()
        setBtnBg(BtnR, TH.redDark)
        startStealPath(stealPathRight)
    end
end)

-- ═══════════════════════════════════════════════
-- TOOL BUTTONS
-- ═══════════════════════════════════════════════

ARBtn.MouseButton1Click:Connect(function()
    if AntiRagdollEnabled then
        disableAntiRagdoll()
        ARBtn.Text = "Anti Ragdoll: OFF"
        setBtnBg(ARBtn, TH.rowBg)
    else
        enableAntiRagdoll()
        ARBtn.Text = "Anti Ragdoll: ON"
        setBtnBg(ARBtn, TH.redDark)
    end
end)

JumpBtn.MouseButton1Click:Connect(function()
    if jumpActive then
        deactivateJump()
        JumpBtn.Text = "Infinite Jump: OFF"
        setBtnBg(JumpBtn, TH.rowBg)
    else
        activateJump()
        JumpBtn.Text = "Infinite Jump: ON"
        setBtnBg(JumpBtn, TH.redDark)
    end
end)

SpeedBoostBtn.MouseButton1Click:Connect(function()
    toggleSpeedBoost()
    if SpeedBoostActive then
        SpeedBoostBtn.Text = "Speed Boost: ON"
        setBtnBg(SpeedBoostBtn, TH.redDark)
    else
        SpeedBoostBtn.Text = "Speed Boost: OFF"
        setBtnBg(SpeedBoostBtn, TH.rowBg)
    end
end)

UnwalkBtn.MouseButton1Click:Connect(function()
    if UnwalkActive then
        stopUnwalk()
        UnwalkBtn.Text = "Unwalk: OFF"
        setBtnBg(UnwalkBtn, TH.rowBg)
    else
        startUnwalk()
        UnwalkBtn.Text = "Unwalk: ON"
        setBtnBg(UnwalkBtn, TH.redDark)
    end
end)

-- ═══════════════════════════════════════════════
-- AUTO STEAL BAR
-- ═══════════════════════════════════════════════

local stealBarW = 280
local stealBarH = 36

local FrameStealBar = Instance.new("Frame", Gui)
FrameStealBar.Name = rotateName(NamePool.Frame, "StealBar")
FrameStealBar.Size = UDim2.new(0, stealBarW * Scale, 0, stealBarH * Scale)
FrameStealBar.Position = UDim2.new(0.5, -(stealBarW * Scale) / 2, 1, -(stealBarH + 138) * Scale)
FrameStealBar.BackgroundColor3 = TH.bg
FrameStealBar.BackgroundTransparency = 0.3
FrameStealBar.BorderSizePixel = 0
makeCorner(FrameStealBar, 8)
createAnimatedBorder(FrameStealBar, 1.5, 2)

local sPad = 6

local stealTitleLbl = Instance.new("TextLabel", FrameStealBar)
stealTitleLbl.Size = UDim2.new(0, 72 * Scale, 0, 17 * Scale)
stealTitleLbl.Position = UDim2.new(0, sPad * Scale, 0, 2 * Scale)
stealTitleLbl.BackgroundTransparency = 1
stealTitleLbl.Text = "AUTO STEAL"
stealTitleLbl.Font = Enum.Font.GothamBold
stealTitleLbl.TextSize = 10 * Scale
stealTitleLbl.TextColor3 = TH.redBright
stealTitleLbl.TextXAlignment = Enum.TextXAlignment.Left
stealTitleLbl.ZIndex = 3
createAnimatedTextGradient(stealTitleLbl, 2)

-- RAD Controls
local RadLabel = makeStyledLabel(FrameStealBar, sPad + 84, 2, 24, 17, "RAD", 8)
local RadMinusBtn = makeBtn(FrameStealBar, sPad + 84 + 26, 2, 20, 17, "-", 11)
local RadValueLbl = makeStyledLabel(FrameStealBar, sPad + 84 + 48, 2, 22, 17, tostring(StealSettings.Radius), 9)
local RadPlusBtn = makeBtn(FrameStealBar, sPad + 84 + 72, 2, 20, 17, "+", 11)

-- DUR Controls
local DurLabel = makeStyledLabel(FrameStealBar, sPad + 84 + 100, 2, 24, 17, "DUR", 8)
local DurMinusBtn = makeBtn(FrameStealBar, sPad + 84 + 126, 2, 20, 17, "-", 11)
local DurValueLbl = makeStyledLabel(FrameStealBar, sPad + 84 + 148, 2, 30, 17, string.format("%.2f", StealSettings.Duration), 9)
local DurPlusBtn = makeBtn(FrameStealBar, sPad + 84 + 180, 2, 20, 17, "+", 11)

-- Visual Toggle
local VisualToggle = makeBtn(FrameStealBar, sPad, 21, 74, 12, "VISUAL: OFF", 9)

-- Progress Bar
local ProgressBg = Instance.new("Frame", FrameStealBar)
ProgressBg.Size = UDim2.new(1, -(sPad * 2 + 74) * Scale, 0, 4)
ProgressBg.Position = UDim2.new(0, (sPad + 80) * Scale, 0, 25 * Scale)
ProgressBg.BackgroundColor3 = TH.rowBg
ProgressBg.BackgroundTransparency = 0.3
ProgressBg.BorderSizePixel = 0
makeCorner(ProgressBg, 2)

ProgressBarFill = Instance.new("Frame", ProgressBg)
ProgressBarFill.Size = UDim2.new(0, 0, 1, 0)
ProgressBarFill.BackgroundColor3 = TH.redBright
ProgressBarFill.BorderSizePixel = 0
makeCorner(ProgressBarFill, 2)

-- Button Events
RadMinusBtn.MouseButton1Click:Connect(function()
    StealSettings.Radius = math.max(1, StealSettings.Radius - 1)
    RadValueLbl.Text = tostring(StealSettings.Radius)
    updateRadiusBallSize()
end)

RadPlusBtn.MouseButton1Click:Connect(function()
    StealSettings.Radius = math.min(30, StealSettings.Radius + 1)
    RadValueLbl.Text = tostring(StealSettings.Radius)
    updateRadiusBallSize()
end)

DurMinusBtn.MouseButton1Click:Connect(function()
    StealSettings.Duration = math.max(0.05, StealSettings.Duration - 0.05)
    DurValueLbl.Text = string.format("%.2f", StealSettings.Duration)
end)

DurPlusBtn.MouseButton1Click:Connect(function()
    StealSettings.Duration = math.min(2, StealSettings.Duration + 0.05)
    DurValueLbl.Text = string.format("%.2f", StealSettings.Duration)
end)

-- ═══════════════════════════════════════════════
-- AUTO STEAL LOGIC
-- ═══════════════════════════════════════════════

scanPlots()

AutoStealConn = Services.RunService.Heartbeat:Connect(function()
    if IsStealing then return end
    local nearest = getNearest()
    if nearest then
        local prompt = getPrompt(nearest)
        if prompt then
            executeStealInstant(prompt)
        end
    end
end)

-- Visual Toggle
VisualToggle.MouseButton1Click:Connect(function()
    VisualActive = not VisualActive
    if VisualActive then
        createRadiusBall()
        VisualToggle.Text = "VISUAL: ON"
        setBtnBg(VisualToggle, TH.redDark)
    else
        if RadiusBall then
            pcall(function() RadiusBall:Destroy() end)
            RadiusBall = nil
        end
        VisualToggle.Text = "VISUAL: OFF"
        setBtnBg(VisualToggle, TH.rowBg)
    end
end)

-- Radius Ball Update
RadiusBallConn = Services.RunService.Heartbeat:Connect(function()
    if VisualActive and RadiusBall and RadiusBall.Parent then
        updateRadiusBall()
    end
end)

-- Periodic Rescan
task.spawn(function()
    while Gui and Gui.Parent do
        scanPlots()
        task.wait(1.5)
    end
end)

-- ═══════════════════════════════════════════════
-- CLOSE ALL
-- ═══════════════════════════════════════════════

CloseAllBtn.MouseButton1Click:Connect(function()
    scriptAlive = false
    if FPSConn then FPSConn:Disconnect(); FPSConn = nil end
    if AutoStealConn then
        AutoStealConn:Disconnect()
        AutoStealConn = nil
    end
    stopStealPath()
    currentPathSide = nil
    if BtnL_Ref and BtnL_Ref.Parent then setBtnBg(BtnL_Ref, TH.rowBg) end
    if BtnR_Ref and BtnR_Ref.Parent then setBtnBg(BtnR_Ref, TH.rowBg) end
    disableAntiRagdoll()
    deactivateJump()
    if SpeedBoostActive then
        SpeedBoostActive = false
        local hum = getHumanoid()
        if hum then hum.WalkSpeed = savedWalkSpeed end
        if SpeedBoostConn then
            SpeedBoostConn:Disconnect()
            SpeedBoostConn = nil
        end
    end
    pcall(function()
        if MusicSound2 then
            MusicSound2:Stop()
            MusicSound2:Destroy()
        end
    end)
    LockActive = false
    stopLock()
    if LockBtn_Ref and LockBtn_Ref.Parent then
        LockBtn_Ref.Text = "Lock"
        setBtnBg(LockBtn_Ref, TH.rowBg)
    end
    if UnwalkActive then
        stopUnwalk()
    end
    VisualActive = false
    if RadiusBall then
        pcall(function() RadiusBall:Destroy() end)
        RadiusBall = nil
    end
    if RadiusBallConn then RadiusBallConn:Disconnect(); RadiusBallConn = nil end
    DropActive = false
    for _, c in ipairs(DropConns) do
        pcall(function() if typeof(c) == "RBXScriptConnection" then c:Disconnect() end end)
    end
    DropConns = {}
    pcall(function()
        if WatermarkGui and WatermarkGui.Parent then WatermarkGui:Destroy() end
    end)
    pcall(function()
        if SoundParent and SoundParent.Parent then SoundParent:Destroy() end
    end)
    Gui:Destroy()
    GuiRef = nil
end)

print("[Nuxus Hub] Loaded successfully!")
